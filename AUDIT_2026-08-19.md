# AUDIT_2026-08-19.md — 레이아웃 엔진 전면 감사 보고서

> **감사 일시**: 2026-08-19
> **감사 범위**: `src/engine/`, `src/edit/`, `src/components/`, `src/resource/`, `src/utils/`, `src/types/` 전체 + 엔진↔DOM 통합 seam
> **감사 방법**: 5개 `deep` 에이전트 병렬 실행 (총 ~35분), 각 파일 line-by-line + `docs/*.md` 교차 검증
> **검증 환경**: `npx tsc --noEmit` 통과, `npm run verify:engine` 25개 통과, 테스트 러너 미설정
> **비고**: 본 보고서는 감사만 수행, 코드 수정 없음. 모든 발견사항은 file:line + 심각도 + 재현 방법 포함.

---

## 목차

- [검증 한계](#검증-한계)
- [종합 통계](#종합-통계)
- [우선 대응 권장사항](#우선-대응-권장사항-위험도-순)
- [Part 1. 심각한 버그 (Critical Bugs)](#part-1-심각한-버그-critical-bugs)
- [Part 2. 잠재적/숨겨진 버그 (Potential/Latent Bugs)](#part-2-잠재적숨겨진-버그-potentiallatent-bugs)
- [Part 3. 임시방편/주먹구구식 코드 (Band-Aid/Patch-Up)](#part-3-임시방편주먹구구식-코드-band-aidpatch-up)
- [Part 4. 불필요/데드 루틴 (Dead/Unnecessary)](#part-4-불필요데드-루틴-deadunnecessary)
- [Part 5. 미구현/불완전 (Incomplete Implementations)](#part-5-미구현불완전-incomplete-implementations)
- [Part 6. 문서-코드 불일치 (Doc/Code Drift)](#part-6-문서-코드-불일치-doccode-drift)
- [Part 7. 통합 계층 특화 (Engine↔DOM Seam)](#part-7-통합-계층-특화-enginedom-seam)
- [Appendix A. 엔진 계층 상세 보고서](#appendix-a-엔진-계층-상세-보고서)
- [Appendix B. 편집 계층 상세 보고서](#appendix-b-편집-계층-상세-보고서)
- [Appendix C. 컴포넌트 계층 상세 보고서](#appendix-c-컴포넌트-계층-상세-보고서)
- [Appendix D. 리소스/유틸/타입 계층 상세 보고서](#appendix-d-리소스유틸타입-계층-상세-보고서)

---

## 검증 한계

- **테스트 러너 없음**: `vitest`/`jest` 미설정. `AGENTS.md`에 "No tests exist" 명시. 재현은 코드 검사 + 수동 스크립트 기반.
- **엔진 검증 스크립트**: `npm run verify:engine` 25개 통과, 하지만 본 보고서의 엣지 케이스 미커버.
- **DOM 동작 검증**: 브라우저 수동 테스트 필요, 자동화 미구축.
- **일부 대용량 파일**: 도구 출력 truncation으로 일부 line 번호가 근사치일 수 있음 (`paragraph-engine.ts` ~60KB, `edit-manager.ts` ~105KB, `layout-edit-controller.ts` ~114KB, `text-edit-controller.ts` ~90KB).
- **감사 기준**: `AGENTS.md`의 계약 + `docs/*.md`의 명세를 권위로 간주. 코드가 docs와 다르면 drift로 보고.

---

## 종합 통계

| 카테고리                       | 건수 | 최고 심각도 |
| ------------------------------ | ---- | ----------- |
| 심각한 버그 (Critical)         | 22   | Critical    |
| 잠재적/숨겨진 버그 (Potential) | 34   | High        |
| 임시방편 코드 (Band-Aid)       | 18   | Low~Medium  |
| 불필요/데드 루틴 (Dead)        | 17   | Low         |
| 미구현/불완전 (Incomplete)     | 12   | High        |
| 문서-코드 불일치 (Drift)       | 20   | Critical    |
| 통합 계층 특화 (Seam)          | 8    | High        |
| **총계**                       | **131** |             |

---

## 우선 대응 권장사항 (위험도 순)

### 즉시 수정 필요 (Critical, 모든 사용자 영향)

1. **ColorRegistry CMYK→RGB 공식修正** (C1.1) — 모든 인쇄 출력물 색상 오염
2. **ParagraphEngine 캐시 해시에 rgbaData/overlapPadding 추가** (C1.7, S5) — 이미지 로드 후 wrapping 미갱신
3. **TextEditContextAdapter 연결 또는 제거** (C1.10, I5.8) — dead code이면서 docs가 활성이라 거짓 명시
4. **IME Escape 롤백 구현** (C1.12) — 편집 중 조합 문자 잔류
5. **paragraph.content TextBlockData[] 처리** (C1.18) — rich content 갱신 무시
6. **LayoutEditController reparent 상태 초기화** (C1.13) — 재드래그 시 좌표 오류

### 단기 대응 (High, 특정 시나리오 영향)

7. **BoxEngine.absHeight padding 이중 차감修正** (C1.5) — 중첩 static 박스 붕괴
8. **paragraph/image 엔진 owner 검증** (C1.16) — DOM 요소 교체 시 stale 엔진
9. **paragraph.data setter scheduleRender 추가** (C1.17) — 직접 할당 시 DOM stale
10. **Table row resize Escape _rows 복원** (C1.21) — resize 취소 무효
11. **Image engine geometry update 누락** (S3) — inheritStyle/좌표 변경 시 overlap/printPostData stale
12. **편집 중 data 재할당 model.textContent 보존** (S7) — undo/redo/React prop update 시 편집 소실
13. **cursorMove key repeat 구현** (C1.11, I5.7) — 커서 위치 UI stale

### 중기 대응 (Medium, 엣지 케이스/구조적 부채)

14. GridCalculatorEngine explicit-column editableHeight floor (C1.4, I5.5)
15. 0-width 이미지 overlap 0으로 나눔 가드 (C1.9)
16. getCharRect stripped space 인덱스修正 (C1.8)
17. contentElement table content 처리 (C1.22, I5.1)
18. objectFit docs/code 불일치 해소 (D6.12, D6.13, S4) — docs 수정 또는 코드 수정
19. EditManager singleton→per-document docs 정정 (D6.6)
20. Host z-index +100 offset 재검토 (C1.20, D6.18)

---

## Part 1. 심각한 버그 (Critical Bugs)

> 총 22건. 로직 오류, 잘못된 결과 산출, 또는 크래시를 유발하는 버그.

### C1.1 ColorRegistry CMYK→RGB 변환 공식이 틀림

- **파일**: `src/resource/color-registry.ts:104-119`
- **심각도**: Critical (모든 출력물 색상 오염)
- **문제**: 표준 CMYK→RGB 변환 `r = round(255 * (1 - c/100) * (1 - k/100))` 대신 `r = round(255 * (1 - min(1, c + k)))` 사용. 검은 잉크(k)가 포함된 모든 중간 톤이 검은색으로 붕괴됨.
- **재현**: `ColorRegistry.getCSSColor('cyan')` for `{c:128, m:0, y:0, k:128}` → 현재 `#000000`, 정답 ~`#404040`. 인쇄 출력물의 모든 혼합 색상이 오염됨.
- **docs drift**: `docs/RESOURCE.md`는 "CMYK → RGB 변환"이라 명시하나 공식 미명시. 코드 동작이 일반 인쇄 기대와 상이.

### C1.2 ColorRegistry `get('default')`가 등록된 색이 아닌 흰색 반환

- **파일**: `src/resource/color-registry.ts:209-211`
- **심각도**: Critical
- **문제**: `get(name)`이 `_colorSet[name] || _defaultColor`를 반환. init 후 `_defaultColor`는 `{c:0,m:0,y:0,k:255}`(검은색)이나, `_defaultColor` 초기값은 `{c:0,m:0,y:0,k:0}`(흰색). `getCSSColor('default')`는 `Object.keys(_colorSet).includes(name)`로 등록 여부 확인 가능하지만 `get()`은 그렇지 않음 → API surface 불일치.
- **재현**: `{ default: {c:0,m:0,y:0,k:0} }` 등록 후 `registry.get('default')` 호출 → `_defaultColor`(흰색) 반환. `registry.getCSSColor('default')`는 등록된 색 반환.

### C1.3 FontLoader.init() screen mode 서명 비교가 잘못됨

- **파일**: `src/resource/font-loader.ts:116-125, 240-243`
- **심각도**: Critical
- **문제**: non-print mode에서 `init(fonts)`는 호출자 전달 `fonts`를 무시하고 `_loadServer()`에서 fetch. 그 후 `_lastFontsSignature`를 fetched 배열로 기록. 하지만 early-exit 검사는 **호출자 인자**(무시된 값)로 `candidateSignature`를 계산. 두 가지 버그:
  1. 동일 인자로 두 번 호출 시 서버 `fonts.json`이 변경되어도 재fetch하지 않음.
  2. 첫 호출 `undefined` 시 `candidateSignature` undefined → guard bypass; 동일 undefined 재호출 시 어차피 재실행(캐시 무효).
- **재현**: `await FontLoader.getInstance().init(someFonts)` 호출 후 서버 `fonts.json` 변경, 동일 `someFonts`로 재호출 → stale `_fontFaces` 반환, refetch 안 함.

### C1.4 GridCalculatorEngine explicit-column 경로가 editableHeight 내림을 생략

- **파일**: `src/engine/grid-calculator-engine.ts:121`
- **심각도**: Critical
- **문제**: numeric-column 분기(line 102)는 `Math.floor((height - padding) / lineHeight) * lineHeight` 사용. explicit-column(`columns: number[]`) 분기는 `height - padding` 그대로 사용. docs는 `editableHeight`가 항상 lineHeight 정수배라 명시. fractional 마지막 줄 발생 → overflow/red-shadow 임계값이 동일 크기 문서에서 서로 다름.
- **재현**: `columns: [40,40,40]`, `paddingTop:0`, `paddingBottom:0`, `lineHeight=4.8`, `height=370` → `editableHeight=370`(정수배 아님) vs equal-column은 `369.6`.
- **docs drift**: `docs/ENGINE.md §2.2`가 "lineHeight의 정수배로 내림" 명시, 위반.

### C1.5 BoxEngine.absHeight static clamp가 padding을 이중 차감

- **파일**: `src/engine/box-engine.ts:208-210`
- **심각도**: Critical
- **문제**: `calcHeight = Math.min(calcHeight, contentHeight - (this.relTop - (this._data.paddingTop ?? 0)))`. `relTop` for static은 `coord.y1 + lineHeight * top`이고 `coord.y1`은 부모 grid `paddingTop` 포함. `_data.paddingTop`은 자식 박스 자체 패딩(보통 0). 부모 `paddingTop=5`, 자식 `top=3` → `relTop ≈ 5 + 3*lineHeight`, `contentHeight - relTop`가 음수 → `Math.max(0, calcHeight)`가 0 반환 → 박스 붕괴.
- **재현**: 부모 박스 `paddingTop: 5`, 자식 static 박스 `top: 3, height: 5` → 자식 높이 0으로 붕괴.

### C1.6 DocumentEngine._buildBoxEngine reparent 시 엔진 이중 부모 참조

- **파일**: `src/engine/document-engine.ts:269`
- **심각도**: Critical
- **문제**: `existingBox.parent = parent`가 기존 부모의 `childEngines`에서 제거하지 않고 새 부모를 가리킴. 동일 id 박스가 다른 컨테이너로 이동 시 기존 부모의 `printPostData`가 여전히 이 엔진을 순회하면서 새 부모 좌표계로 잘못된 absRect 계산.
- **재현**: `DocumentEngine.layout()`에 새 data로 기존 id를 다른 컨테이너로 이동. `existingBox.parent = parent` mutation 후 기존 부모 `childEngines`에 여전히 동일 객체 → 두 부모가 동일 child 소유.

### C1.7 ParagraphEngine 레이아웃 캐시가 rgbaData/overlapPadding 변경을 감지하지 못함

- **파일**: `src/engine/paragraph-engine.ts:882-924` (해시), `:603-607` (캐시 히트)
- **심각도**: Critical
- **문제**: `_computeLayoutInputHash`가 overlay `absLeft/absTop/absWidth/absHeight`, `overlapMode`는 포함하지만, **rgbaData 존재 여부**와 **overlapPadding**은 미포함. 이미지 로드 후 rgbaData가 주입되어도 해시 동일 → 캐시 히트 → 기하학적 fallback 결과 재사용.
- **재현**:
  1. `overlapMode: 'path'` 이미지를 paragraph 위에 배치.
  2. 이미지 로드 전 `paragraph.render()` 실행 → geometric fallback 결과 캐시 저장.
  3. 이미지 로드 후 `_notifyOverlapParagraphs()` 트리거 → paragraph 재렌더링.
  4. 해시 동일(위치/모드 unchanged) → 캐시 히트 → 픽셀 정밀 wrapping 미수행.
- **docs**: AGENTS.md는 `_notifyOverlapParagraphs()`가 `resetIncrementalState()` 트리거라 명시하나, DOM layer 경로만 해당. 순수 `ParagraphEngine` 사용자가 rgbaData 수동 주입 시 캐시 무효화 트리거 없음.

### C1.8 ParagraphEngine.getCharRect가 stripped space 인덱스를 잘못 계산

- **파일**: `src/engine/paragraph-engine.ts:1214-1226`, `_computeCharOffsets:342-349`
- **심각도**: Critical
- **문제**: `charOffsets`는 stripped(leading/trailing space 제거) 범위만 커버. `getCharRect`가 `charOffsets[localIdx]`를 raw content 인덱스로 조회. `localIdx < stripStart` 시 `charOffsets[0]`는 첫 non-space char의 offset → leading space 위치가 다음 char offset으로 반환. trailing space(`localIdx >= stripEnd`) 시 `charOffsets[localIdx]` undefined → `charLeftInPart=0`, `charWidth=part.width - charOffsets[stripEnd-1]` → 잘못된 rect.
- **재현**: part content `"안녕  "` with `endOfBlock=false`. 첫 trailing space(`localIdx=2`) 조회 → `charOffsets[2]` undefined → rect left=0, width=part.width-`charOffsets[1]` → 잘못된 커서 위치.

### C1.9 overlap-engine / image-engine 0-width 이미지에서 0으로 나눔

- **파일**: `src/engine/overlap-engine.ts:140`, `src/engine/image-engine.ts:208`
- **심각도**: Critical
- **문제**: `scaleX = rgba.width / imgRectMm.width`. `imgRectMm.width === 0` 시 `scaleX = Infinity`, 후속 `Math.floor/ceil`이 `NaN` 생성. bounds 검사 `sw <= 0`가 `NaN`에 대해 false → 전체 버퍼 스캔 → 쓰레기 parts. `scaleY` 동일.
- **재현**: `absWidth=0` 또는 `absHeight=0` 이미지 박스 + `overlapMode='path'` → NaN/Infinity 전파.

### C1.10 TextEditContextAdapter가 완전 구현되었으나 한 번도 인스턴스화되지 않음

- **파일**: `src/edit/text-edit-context-adapter.ts:60-72`, `src/edit/text-edit-controller.ts:80-115`
- **심각도**: Critical
- **문제**: `codegraph_callers` 확인 결과 `TextEditContextAdapter.create` 외부 호출자 0건. grep으로 `TextEditContextAdapter`가 자체 파일 외에 출현 0건. `TextEditController` 생성자가 `TextEditCoordinateMapper`는 생성하나 `TextEditContextAdapter.create()`는 결코 호출하지 않음. `EditContextCallbacks.onSelectionChange`(line 164)도 연결 안 됨.
- **재현**: Chromium에서 paragraph 포커스 후 Korean IME composition 시작 → EditContext 경로가 아닌 hidden `<textarea>` fallback 경로로 동작. adapter 코드는 존재하나 도달 불가.
- **영향**: docs가 EditContext 사용을 명시하나 실제로는 textarea fallback. EditContext가 해결하려 한 IME 좌표 reporting 등 미해결.

### C1.11 cursorMove 이벤트가 key repeat 중 전혀 발생하지 않음

- **파일**: `src/edit/text-edit-controller.ts:873-875`, `docs/EDITING_TEXT.md:380-397`
- **심각도**: Critical
- **문제**: `_onKeydown`이 `!event.repeat && isCursorKey` 조건으로 `cursorMove` dispatch. 반복 화살표 키에서 `event.repeat === true` → `cursorMove` 0회 발생. docs는 "키보드 연속 입력 시 최초 KeyDown과 마지막 KeyUp에만 발생" 명시하나, keyup에도 `cursorMove` 발생 안 함.
- **재현**: `ArrowRight` 길게 누르고 `manager.addEventListener('cursorMove')` 로깅 → keydown 1회만 발생, 반복 tick에서 0회, keyup에서 0회.
- **영향**: 외부 UI의 커서 위치 표시기가 화살표 키 이동 중 갱신되지 않음.

### C1.12 IME composition 중 Escape가 model.textContent를 롤백하지 않음

- **파일**: `src/edit/text-edit-controller.ts:774-783`
- **심각도**: Critical
- **문제**: composition 중 Escape → `_isComposing = false`, composition span 제거, 커서 재위치. 그러나 `model.textContent`/`textarea.value`를 pre-composition 문자열로 복원하지 않음. `_onCompositionEnd`가 트리거되지 않으므로 롤백 경로 없음.
- **재현**: Korean composition 시작, 부분 입력, Escape → 미완성 조합 문자가 `textarea.value`와 `model.textContent`에 잔류.
- **영향**: IME cancel이 미완성 조합 문자를 문서에 누출.

### C1.13 LayoutEditController reparent drag 상태가 재사용 시 초기화되지 않음

- **파일**: `src/edit/layout-edit-controller.ts:794-905`
- **심각도**: Critical
- **문제**: `state.reparentOutside`가 박스가 부모 밖으로 나갈 때 설정됨. mouseup 후 `_tryReparent` 실행되어도 `state.reparentOutside`가 `null`로 reset되지 않음. `Map`에서 동일 박스의 state 객체가 후속 드래그에 재사용.
- **재현**: reparent mode에서 박스를 부모 밖으로 드래그(`reparentOutside` 설정), mouseup. 동일 박스를 다시 부모 내부에서 드래그 → stale `reparentOutside`가 `_computeNewPosition`에서 사용 → 초기 위치 오류.
- **영향**: 동일 박스 재드래그 시 좌표 점프.

### C1.14 _suppressLayoutClick 타이머(200ms)가 dblclick 임계값(~300ms)보다 짧음

- **파일**: `src/edit/edit-manager.ts:1856-1875`
- **심각도**: Critical
- **문제**: drag/insert/resize 완료 후 `_suppressLayoutClick()`이 200ms window capture listener 추가. 브라우저 기본 dblclick 임계값은 ~300ms. 첫 click은 억제되나, 200ms 이후 두 번째 click은 억제 리스너 소멸 후 도달 → 의도치 않은 selection 변경.
- **재현**: drag/insert/resize 후 빠르게 두 번 click → 첫 click 억제, 두 번째 click은 200ms 이후 → `LayoutSelectionController` 도달 → selection 변경.

### C1.15 LayoutParagraphElement.render()가 ParagraphEngine.scale을 설정하지 않음

- **파일**: `src/components/layout/paragraph.element.ts:292-373`, `docs/TEXT_ENGINE.md §12.5.1, §13.4`
- **심각도**: Critical
- **문제**: docs는 `LayoutParagraphElement.render()`가 `layoutDocEl.editManager.scale`을 읽어 `model.scale`에 할당한다 명시. 컴포넌트 코드는 엔진을 생성/실행하나 `this._model.scale`을 결코 할당하지 않음.
- **재현**: `editManager.setScale(2)` 후 박스 resize → paragraph reflow 위치가 scale=1과 다르고 커서/selection 좌표 drift.
- **영향**: scale 독립 렌더링 미달성.

### C1.16 paragraph/image 엔진 재사용이 owner 검증 없이 첫 엔진 반환

- **파일**: `src/components/layout/paragraph.element.ts:133-135`, `src/components/layout/image.element.ts:500-501`
- **심각도**: Critical
- **문제**: `_layoutStructure()`가 `parentBoxEngine?.childEngines.find(e => e instanceof ParagraphEngine)`(또는 `ImageEngine`)로 엔진 검색. `this` 소유 여부 미검증. paragraph/image 제거 후 새 요소 추가 시 새 요소가 이전 분리된 엔진(이전 `textContent`/`rgbaData`/`absRect` 보유) 재사용. `disconnectedCallback()`이 부모 `BoxEngine.childEngines`에서 엔진 제거하지 않음.
- **재현**: 박스에서 paragraph 자식 제거, 다른 text로 새 paragraph 추가 → 새 paragraph가 이전 text와 이전 overlap shape으로 렌더링, 전체 부모 재구축까지 지속.

### C1.17 LayoutParagraphElement.data setter가 render를 스케줄하지 않음

- **파일**: `src/components/layout/paragraph.element.ts:481-495`
- **심각도**: Critical
- **문제**: setter가 `this.layout()` 호출 + `_perfStructureChanged = true` 설정하나, `scheduleRender()`/`markStructureChangedAndRender()` 미호출. 호스트가 `paragraph.data = {...}` 직접 할당 시 model은 갱신되나 `render()` 미실행.
- **재현**: paragraph 생성 후 `paragraph.data = { type:'paragraph', content:'new text', column:2 }` → column DOM이 이전 content 표시.
- **docs drift**: `docs/API.md §Paragraph data setter`가 full re-creation 명시.

### C1.18 paragraph.content setter가 TextBlockData[] 배열을 무시

- **파일**: `src/components/layout/paragraph.element.ts:559-563`
- **심각도**: Critical
- **문제**: setter가 `typeof value === 'string'`일 때만 `this._model.textContent = value`. `TextBlockData[]`은 `_sourceContent`만 갱신. `_layoutStructure()`가 `this._model?.textContent ?? this._sourceContent` 우선이므로 model의 stale 값이 승리 → 새 rich content 무시.
- **재현**: 첫 렌더링 후 `paragraph.content = [{ content:'bold', textBlockStyle:{ fontWeight:700 } }]` → 렌더링된 text가 이전 string 그대로, font-weight block 상실.
- **docs drift**: `docs/API.md §Paragraph content setter`가 `TextBlockData[]` 타입 허용 명시.

### C1.19 visibleGuide setter가 _visibleGuide 값을 저장하지 않음

- **파일**: `src/components/layout/document.element.ts:658-667`
- **심각도**: High
- **문제**: `set visibleGuide(value)`가 기존 guide 요소의 `visible` property만 토글, `this._visibleGuide = value` 미할당. `_renderGuideColumns()`가 `this._visibleGuide` 사용 → 다음 `layout()`/`data` setter 시 stale 값으로 guide 재생성.
- **재현**: `doc.visibleGuide = false; doc.layout();` → guide column이 다시 나타남(`_visibleGuide`가 여전히 `true`).

### C1.20 Host z-index +100 offset이 role 기반 값을 UI 예약 범위로 밀어넣음

- **파일**: `src/components/layout/box.element.ts:452`, `paragraph.element.ts:256`, `image.element.ts:211`
- **심각도**: High
- **문제**: `z-index: ${this.zIndex + 100}`. role `ad`(91000) → 91100, `header`(91001) → 91101. AGENTS.md는 90001~99999를 UI chrome 예약으로 명시. 레이아웃 데이터가 UI chrome 범위 침범. 일반 `zIndex=90000`도 90100으로 UI 범위 진입.
- **재현**: `role='ad'` 박스 할당, host element 검사 → `z-index: 91100`.
- **docs drift**: `AGENTS.md §z-index range` 위반.

### C1.21 Table row resize Escape가 _rows를 복원하지 않음

- **파일**: `src/components/layout/table.element.ts:710-731`
- **심각도**: High
- **문제**: row resize 중 `_applyRowResize()`가 `this._rows[topIdx].height`를 변형. `_onTableResizeKeyDown`의 Escape가 `trEl.height = startRowHeights[i]`만 복원, `this._rows[i].height` 미복원. 다음 `_layoutStructure()`가 `_rows`에서 엔진 재구축 → 다시 변형된 높이 TR에 기록.
- **재현**: table row resize, Escape, 빈 곳 click으로 layout 트리거 → row가 drag한 높이로 복귀.

### C1.22 LayoutBoxElement.contentElement가 table content를 잘못 타입 캐스트

- **파일**: `src/components/layout/box.element.ts:1352-1368`
- **심각도**: High
- **문제**: `contentElement` getter가 `ParagraphEngine`/`ImageEngine`만 분기. 나머지는 자식 박스 재귀 후 `LayoutImageElement | LayoutParagraphElement`로 캐스트. table content 시 table 요소가 잘못된 타입 반환. overlap 수집 코드가 `overlapMode` 접근 → table에서 undefined.
- **재현**: `box.data = { type:'box', children:{ type:'table', rows:[...] } }` 후 `box.contentElement` 읽기 또는 paragraph overlap 재계산 트리거.

---

## Part 2. 잠재적/숨겨진 버그 (Potential/Latent Bugs)

> 총 34건. 일반 케이스는 통과하나 엣지 케이스에서 오작동.

### 엔진 계층 (P2.1 ~ P2.12)

#### P2.1 BoxEngine.contentType이 혼합 자식에서 'table' 우선 반환

- **파일**: `src/engine/box-engine.ts:230-241`
- **심각도**: Medium
- **문제**: `_childEngines.length !== 1`이고 table 자식이 하나라도 있으면 `contentType`이 `'table'` 반환. DOM wrapper는 박스당 단일 content 요소 기대. image + table 혼합 시 `contentType='table'`, overlay/printPostData가 table으로 처리하나 실제 render path는 정의되지 않음.
- **재현**: `BoxData.children`에 image 박스와 table 박스 혼합 → `contentType='table'`, `overlayElements` 필터가 `contentElement`를 `ParagraphEngine | null`로 캐스트 → 혼합 자식 미가드.

#### P2.2 BoxEngine.overlayElements가 부모 overlay를 교집합 필터 없이 추가

- **파일**: `src/engine/box-engine.ts:262-287`
- **심각도**: Medium
- **문제**: `list.push(...this._parent.overlayElements)`가 무조건 추가. docs는 `overlayElements`가 "sibling boxes with higher z-index and intersecting space"라 명시. 부모 overlay가 현재 박스와 교집합 없어도 추가 → `_detectOverlapWithCache`가 대부분 reject하나 O(depth × overlays) 낭비.
- **재현**: 깊이 중첩 paragraph + 다수 root image → 각 `layoutText()`가 root image overlay 전체 스캔.

#### P2.3 GridCalculatorEngine 음수 editableWidth 미검증

- **파일**: `src/engine/grid-calculator-engine.ts:101-103`
- **심각도**: Medium
- **문제**: `editableWidth = width - paddingLeft - paddingRight - gaps.reduce(...)`. padding + gap > width 시 음수. 음수 columnWidth 전파 → `ParagraphEngine`이 NaN/negative parts, 가시 text 없음.
- **재현**: `width: 50, paddingLeft: 30, paddingRight: 30, columns: 2, gap: 5` → `editableWidth = -15`, `columnWidth = -7.5`.

#### P2.4 ParagraphEngine force-place가 part width 초과 허용

- **파일**: `src/engine/paragraph-engine.ts:810-815`
- **심각도**: Medium
- **문제**: char가 모든 part보다 넓으면 part 0에 강제 배치, `cumulativeWidths[0]`이 `partWidths[0]` 초과. 다음 char이 새 라인 강제. 의도적 무한루프 가드이나, char가 시각적으로 part 밖으로 overflow.
- **재현**: column width보다 넓은 장식 기호 → column 0에 배치, 시각적 overflow.

#### P2.5 ParagraphEngine._computeCharOffsets stripped space와 global offset 불일치

- **파일**: `src/engine/paragraph-engine.ts:342-349, 358-365`
- **심각도**: Medium
- **문제**: offset은 stripped char만 계산, `getOffsetFromPoint`/`getCharRect`는 `globalOffset`이 `part.content.length`(space 포함)로 증가 기대. `charOffsets`가 `content.length`보다 짧음. 병렬 순회 시 mis-align.
- **재현**: `buildParagraphPrintPostData`가 자체 stripping 재구현, `charOffsets[j] ?? 0` fallback. 로직 divergence 시 조용히 char 위치 shift.

#### P2.6 table-grid-resolver.normalizeWidths 경계 케이스

- **파일**: `src/engine/table-grid-resolver.ts:99-127`
- **심각도**: Medium
- **문제**: `diff === 0` 조기 반환(line 104)이 `inputs.map(Math.max(v, minSize))` 반환. 일부 input이 minSize 미만이고 원래 합이 target과 같을 때, clamped 후 합이 target과 다름에도 재정규화 생략. 실제로는 clamped 합이 target과 다르면 다음 분기로 진행하나, 정확히 같을 확률은 낮음.
- **재현**: `normalizeWidths([1, 49], 50, 5)` → clamped `[5, 49]` 합 54, diff=-4, largest 축소 → `[5, 45]`. 드문 케이스.

#### P2.7 ImageEngine.layout()이 objectFit/originalWidth 무시

- **파일**: `src/engine/image-engine.ts:433-444`
- **심각도**: Medium
- **문제**: `x/y/width/height`를 raw 반환, `objectFit`/`originalWidth/Height` 무시. DOM layer가 별도 `_computeObjectFit` 처리. 엔진 결과는 `printPostData`만 사용 → 의도적일 수 있으나 미문서화.
- **재현**: `objectFit: 'contain'`, `originalWidth/Height` 설정 → `ImageEngine.layout()`이 raw `x/y/w/h` 반환.

#### P2.8 DocumentEngine.data setter가 childBoxEngines 재구축 안 함

- **파일**: `src/engine/document-engine.ts:83-86`
- **심각도**: Low
- **문제**: `set data`가 `_data` + `_gridCalculator`만 갱신, `_childBoxEngines` 미건드림. `layout()` 명시적 호출 필요. 순수 엔진 사용자가 `data`만 설정하고 `childBoxEngines` 읽으면 stale.
- **재현**: `engine.data = newData; console.log(engine.childBoxEngines)` → old boxes.

#### P2.9 ParagraphEngine.getOffsetFromPoint 마지막 column 우측 경계

- **파일**: `src/engine/paragraph-engine.ts:1264`
- **심각도**: Low
- **문제**: `if (relX >= xStart && relX < xEnd)` half-open interval. part 내부는 `<= partRight` 사용. 마지막 column 우측 경계 click이 `null` 반환.
- **재현**: 마지막 column 우측 끝 click → `null`.

#### P2.10 ParagraphEngine.getCursorPlacement(preferLineEnd) 미구현

- **파일**: `src/engine/paragraph-engine.ts:1337-1346`
- **심각도**: Medium
- **문제**: `preferLineEnd=true`가 줄 끝이 아닌 해당 char 우측 반환. `TextEditCoordinateMapper` engine path가 이를 사용 → 줄 끝 커서 배치가 실제 줄 끝이 아님. DOM mapper는 별도 `_lineEndPlacements` 로직.
- **재현**: `paragraph.getCursorPlacement(5, true)` where offset 5 mid-line → offset 5 우측, 줄 끝 우측 아님.

#### P2.11 _LRU Map.keys().next() 구형 타겟 미지원 가능

- **파일**: `src/engine/paragraph-engine.ts:1543`
- **심각도**: Low
- **문제**: `Map.prototype.keys` iterator protocol. Node 18+ / modern browser에서 fine. 구형 타겟이나 특정 transpilation에서 미지원 가능.
- **재현**: 구형 환경에서 `_LRU` eviction 시 TypeError.

#### P2.12 DocumentEngine._refreshParagraphOverlays inheritStyle 미재계산

- **파일**: `src/engine/document-engine.ts:240-259`
- **심각도**: Low
- **문제**: `ce.data = { ...ce.data, overlayEngines }` 후 `resetIncrementalState`/`layoutStructure`/`layoutText`. `data` setter가 `_initLayoutMetrics` 재실행하나 `_buildInheritStyle` 미재실행. 부모 박스 크기 변경 시 `ce.data.inheritStyle` stale. `layout()` 전체 호출 시에는 문제 없음.

### 편집 계층 (P2.13 ~ P2.19)

#### P2.13 _findColumnIndexByOffset 마지막 column 경계

- **파일**: `src/edit/text-edit-coordinate-mapper.ts:758-775`
- **심각도**: Medium
- **문제**: `sourceOffset >= range.end` → `low = mid + 1`. 마지막 column에서 `sourceOffset === lastRange.end` 시 `null` 반환. trailing space/eof 위치가 첫 column fallback으로.
- **재현**: 마지막 column 끝 click → `getSpanByOffset`가 `null`, cursor fallback.

#### P2.14 useEngineCoordinateQueries 영구 false

- **파일**: `src/edit/text-edit-coordinate-mapper.ts:41, 361-399`
- **심각도**: Medium
- **문제**: `useEngineCoordinateQueries` static default `false`, `true` 할당 코드 전 repo 0건. engine path 코드는 결코 실행되지 않음. DOM path만 production 사용, engine path 정확성 미검증.

#### P2.15 _columnSpansCache가 rebuild() 외에 무효화되지 않음

- **파일**: `src/edit/text-edit-coordinate-mapper.ts:67, 777-790`
- **심각도**: Medium
- **문제**: `_getColumnSpans`가 per-column span array 캐싱. `rebuild()`가 clear하나, 외부 DOM mutation(optimistic span update)은 반영 안 됨.
- **재현**: `_optimisticSpanUpdate` 후 `getSpanByOffset` → 캐시가 교체된 span 반환.

#### P2.16 _onMouseDown이 event.button 미검사

- **파일**: `src/edit/text-edit-controller.ts:570-635`
- **심각도**: Medium
- **문제**: `event.button` 미검사, 모든 버튼 down이 `_isMouseDown = true`. 오른쪽 click도 selection drag 시퀀스 시작.
- **재현**: editable paragraph 내 우 click → `_isMouseDown = true`, context-menu와 충돌.

#### P2.17 EditManager.reset()이 _selectionController detach하지 않음

- **파일**: `src/edit/edit-manager.ts:340-354`, `docs/EDITING_EVENTS.md:109-140`
- **심각도**: Medium
- **문제**: `reset()`이 `layoutEditController`, `insertController`, `placeGunController` destroy하나 `_selectionController` 미detach. docs는 "SelectionController 제외"라 명시. unmount 후 stale listener 잔류.
- **재현**: `LayoutEditor` unmount 후 동일 `EditManager`로 새 마운트 → 구 document의 click/mousedown/dblclick/contextmenu listener 활성.

#### P2.18 Alt-drag clone이 layoutAdd에 source: 'insert' 사용

- **파일**: `src/edit/layout-edit-controller.ts:541-545`
- **심각도**: Medium
- **문제**: Alt-drag clone이 layout edit action이나 `layoutAdd`에 `source: 'insert'` dispatch. `InsertController`와 동일. 외부 UI가 clone과 insert 구분 불가.

#### P2.19 TableKeyboardController.handleTab 도달 불가

- **파일**: `src/edit/table-keyboard-controller.ts:455-507`, `src/components/layout/document.element.ts:222-232`
- **심각도**: Medium
- **문제**: `document.element.ts`가 Tab을 window capture에서 가로채 `navigateByTab` 호출. `table.element.ts` `_onTableKeyDown`이 Tab 수신 불가. `handleTab` public API이나 실제 호출 경로 없음.

### 컴포넌트 계층 (P2.20 ~ P2.28)

#### P2.20 box.data setter가 live HTMLCollection 인덱스 비교

- **파일**: `src/components/layout/box.element.ts:737-739`, `document.element.ts:524-526`, `table.element.ts:191-193`
- **심각도**: Medium
- **문제**: `this.children`이 live `HTMLCollection`. `appendChild` 중 인덱스 shift로 `existingEl !== this.children[i]` 오평가 → 불필요한 DOM 이동. 최종 순서는 보통 맞으나 로직 fragile.

#### P2.21 box.overlayElements AABB가 getBoundingClientRect 사용

- **파일**: `src/components/layout/box.element.ts:1773-1800`
- **심각도**: Low/Medium
- **문제**: 주석이 parent/child transform 상쇄 주장하나 uniform transform에만 성립. CSS scroll, non-uniform transform, scale 변화 시 AABB delta 오류 가능.

#### P2.22 document._onWindowKeyDown이 모든 table selection에 Alt+Arrow 차단

- **파일**: `src/components/layout/document.element.ts:211-221`
- **심각도**: Low
- **문제**: `document.querySelectorAll('x-layout-table')`로 모든 table의 keyboard selection 확인, 포커스된 table 여부 무관. 외부 입력이나 다른 table 조작 중에도 `Alt+Arrow` 차단.

#### P2.23 column.renderText()가 overflow 라인도 span 생성 후 display:none

- **파일**: `src/components/layout/column.element.ts:314-335`
- **심각도**: Low
- **문제**: overflow 라인을 `display:none` span으로 생성. 매우 긴 overflow 시 엔진과 DOM이 모든 hidden 라인 빌드 비용 지불, 조기 절단 없음.

#### P2.24 td._renderPlaceholderBorder 빈 _cellLabels 처리

- **파일**: `src/components/layout/td.element.ts:698-728`
- **심각도**: Low
- **문제**: `_cellLabels` 빈 시 `_getMaxLogicalRow`/`_getMaxLogicalCol`이 0 반환 → 모든 cell이 1×1 grid의 마지막으로 취급, right/bottom placeholder 과잉 그림.

#### P2.25 table._layoutStructure()가 this.children[r] 순서 미검증

- **파일**: `src/components/layout/table.element.ts:332-356`
- **심각도**: Low
- **문제**: `this.children[r]`이 `engineRows[r]` 매치 가정. 수동 DOM 재정렬이나 누락 TR 시 misalign.

#### P2.26 table cell selection overlay가 layout() 시에만 갱신

- **파일**: `src/components/layout/table.element.ts:258-260, 788-841`
- **심각도**: Low
- **문제**: `_renderSelectionOverlay()`가 `layout()`에서만 호출. keyboard controller가 layout 없이 selection 변경 시 overlay lag.

#### P2.27 image.data setter가 overlapMode를 'path'로 reset

- **파일**: `src/components/layout/image.element.ts:467`
- **심각도**: Low
- **문제**: `this._overlapMode = data.overlapMode ?? 'path'`. incoming `ImageData`가 field 미포함 시 이전 값 덮어쓰기.

#### P2.28 _drawImage canvas 크기 Math.round가 sub-pixel 스킵

- **파일**: `src/components/layout/image.element.ts:387-395`
- **심각도**: Low
- **문제**: `Math.round(absWidth * ppm)`가 sub-pixel 크기 변화 스킵 → 이전 canvas dimension 잔류, 약간 부정확한 crop.

### 통합 계층 (P2.29 ~ P2.34)

#### P2.29 Image engine이 URL 변경 후 새 로드 전까지 stale rgbaData 보유

- **엔진**: `src/engine/image-engine.ts:99-101` (`set rgbaData` overwrite only)
- **DOM**: `src/components/layout/image.element.ts:413-418, 479-487` (`_clearImageCache`는 JS image/URL cache만 clear, `_feedRgbaToEngine`은 새 image load 후 async 실행)
- **심각도**: High
- **문제**: URL 변경과 새 image `load` 이벤트 사이에 engine이 이전 image pixel mask 보유. 중간 reflow 시 이전 이미지 shape으로 wrapping.
- **재현**: image A load 완료, `image.data = { ..., url: 'B.png' }`, 즉시 paragraph box resize → A의 rgbaData로 wrapping.

#### P2.30 Image engine data가 inheritStyle/좌표 setter 후 미갱신

- **엔진**: `src/engine/image-engine.ts:76-83` (`set data`만 `_data` 갱신)
- **DOM**: `src/components/layout/image.element.ts:528-550` (x/y/width/height/dpi setter가 `render()`만 호출), `:717-722` (inheritStyle setter도 `_updateEngine()` 미호출)
- **심각도**: High
- **문제**: DOM은 crop/rect 재계산하나 engine은 stale geometry. overlap/printPostData가 old x/y/w/h 사용.
- **재현**: `objectFit: 'cover'`, `originalWidth/Height` 설정 image, 부모 box resize → DOM은 올바른 crop, sibling paragraph는 engine의 stale geometry로 wrapping.

#### P2.31 ImageData 좌표가 docs(source pixels) vs 코드(mm) 불일치

- **엔진**: `src/engine/image-engine.ts:433-444` (x/y/w/h를 mm로 직접 사용, dpi 변환 없음)
- **DOM**: `src/components/layout/image.element.ts:383-407` (`_drawImage`도 mm로 처리)
- **심각도**: Medium
- **문제**: AGENTS.md는 `objectFit !== 'none'` 시 x/y/w/h가 **source image pixels**이고 dpi로 mm 변환한다 명시. 엔진/DOM 모두 mm로 처리. docs 위반.
- **재현**: print mode에서 `objectFit: 'cover'`, x/y/w/h를 source pixels로 전달 → engine `printPostData`가 pixels을 mm로 보고 → post-processing에서 이미지 위치/크기 오류.

#### P2.32 overlapMode: 'none' paragraph가 multi-child group box 내부 시 필터 스킵

- **엔진**: `src/engine/box-engine.ts:271-282` (`contentType === 'paragraph'`일 때만 `overlapMode === 'none'` 필터)
- **DOM**: `src/components/layout/box.element.ts:1252-1263` (동일 로직)
- **심각도**: Medium
- **문제**: multi-child group box의 `contentType`은 `null`. `overlapMode === 'none'` byline paragraph가 group box 내부 시 필터 스킵 → 외부 paragraph가 byline을 overlay로 wrapping.
- **재현**: group box에 byline paragraph(`overlapMode: 'none'`) + 다른 자식, body paragraph가 group box와 overlap → body가 group box rect로 wrapping.
- **docs 위반**: AGENTS.md "when `role === 'byline'`, the inner paragraph's `overlapMode` is set to `'none'` to prevent body paragraphs from treating byline as an overlay" — multi-child group에서 미작동.

#### P2.33 편집 중 data 재할당이 model.textContent 덮어쓰기

- **엔진**: `src/engine/paragraph-engine.ts:1393-1395` (`set textContent` 단순 대체)
- **DOM**: `src/components/layout/paragraph.element.ts:481-495` (`set data`가 `this._model.textContent = data.content`)
- **심각도**: High
- **문제**: `TextEditController`가 편집 내용을 `model.textContent`에 보관, `_sourceContent`는 원본 유지. 외부 caller가 원본 `ParagraphData` 재할당(undo/redo, parent box data 갱신, React prop update) 시 `set data`가 stale `data.content`로 `model.textContent` 덮어쓰기 → 편집 내용 소실.
- **재현**: `editableText` 활성 paragraph에 text 입력, `parentBox.data = parentBox.data` 재할당 → 편집 내용이 원본 content로 복귀.

#### P2.34 빈 table cell에 box 추가 시 첫 layout tick까지 engine orphan

- **엔진**: `src/engine/table-engine.ts:280-292` (`cellChildren.length === 0` 시 `cellEngine.boxEngine` null)
- **DOM**: `src/components/layout/box.element.ts:285-294` (`_findParentEngine`가 `tdParent.engine?.boxEngine ?? null`), `td.element.ts:748-754` (`appendChildData`가 `_layoutStructure()` 후 append)
- **심각도**: Medium
- **문제**: 빈 cell의 `cellEngine.boxEngine`가 null → `_findParentEngine`이 null 반환 → 새 box의 첫 `_layoutStructure`/`_updateEngine`이 engine tree에 attach 불가. 다음 cell `layout()`(MutationObserver 또는 명시적)까지 orphan. 그 전 `absLeft`/`absTop`이 0.

---

## Part 3. 임시방편/주먹구구식 코드 (Band-Aid/Patch-Up)

> 총 18건. TODO/FIXME/hack 주석, 근본 원인 미해결 방어 코드, 문제를 "막아놓기"만 한 부분.

### 엔진 (B3.1 ~ B3.8)

#### B3.1 ParagraphEngine force-place 무한루프 가드

- **파일**: `src/engine/paragraph-engine.ts:810-821`
- **심각도**: Low
- **문제**: char가 part width 초과 시 강제 배치. 무한루프 방지는 필요하나 초과 glyph 사전 분할(근본 해결) 대신 시각적 overflow 허용. docs는 "force-place"라 명시하나 part 밖 렌더링 경고 없음.

#### B3.2 overlap-engine / image-engine geometric fallback

- **파일**: `src/engine/overlap-engine.ts:114-119`, `src/engine/image-engine.ts:173-180`
- **심각도**: Low
- **문제**: `overlapMode === 'path'`이나 rgbaData 없을 시 solid rectangle으로 처리. path mode의 투명 영역 흐름 상실. docs는 한계로 명시하나 근본 해결 아님.

#### B3.3 1e-6 epsilon이 FPU 설정 차이에 따라 다른 결과 가능

- **파일**: `src/engine/paragraph-engine.ts:518, 698, 727, 812, 851`, `overlap-engine.ts:95`, `image-engine.ts:162, 275`
- **심각도**: Low
- **문제**: 부동소수점 edge case 방지용 epsilon. pragma이나 컴파일러/FPU 설정 차이에 따라 다른 줄바꿈 결과 가능.

#### B3.4 minCharWidthMm 미만 free region 폐기

- **파일**: `src/engine/paragraph-engine.ts:537-538`
- **심각도**: Low
- **문제**: `minCharWidthMm = widthRatio * fontSize + letterSpacingEm * fontSize` 미만 free region 폐기, cover로 처리. 얇은 투명 gap 공간 낭비. docs는 의도적 명시.

#### B3.5 ParagraphEngine.scale no-op

- **파일**: `src/engine/paragraph-engine.ts:1472-1480`
- **심각도**: Low
- **문제**: docs는 scale 정규화 명시, 엔진은 DOM 접근 불가 → hard-coded `1`, setter 무시. future bridge stub.

#### B3.6 _initStructureAndMeasureColumns 본체 없음

- **파일**: `src/engine/paragraph-engine.ts:573-575`
- **심각도**: Low
- **문제**: `if (this.columnCount < 1) return;` 후 본체 없음. 3-phase pipeline mirror용 scaffold. 실제 측정은 `data` setter에서 수행.

#### B3.7 ColorRegistryEngineImpl._defaultColor 이중 초기화

- **파일**: `src/engine/color-registry-engine.ts:27, 52`
- **심각도**: Low
- **문제**: 생성자 `{c:0,m:0,y:0,k:0}` → init `{c:0,m:0,y:0,k:255}`. 생성자 값은 dead code.

#### B3.8 FontLoaderEngineImpl parse 실패 조용히 스킵

- **파일**: `src/engine/font-loader-engine.ts:97-99`
- **심각도**: Low
- **문제**: parse 실패 시 해당 폰트만 스킵, 로깅/에러 노출 없음. silent degradation. `getParsedFont`이 `null` 반환으로만 알림.

### 편집 (B3.9 ~ B3.12)

#### B3.9 TextEditController _wasFocused / _pendingTextChangeOnBlur flag

- **파일**: `src/edit/text-edit-controller.ts:74, 84, 391-395, 2025-2029`
- **심각도**: Medium
- **문제**: `_debouncedRender()`가 `_isFocused`를 `_wasFocused`에 snapshot → `postRender()`에서 refocus. `_pendingTextChangeOnBlur`가 `textChange` dispatch를 `_releaseFocus`까지 지연. render/focus/blur 이벤트 순서 근본 미해결 패치. composition, blur, visibility change, render path에서 flag 유지 오류 발생 가능.

#### B3.10 _suppressLayoutClick window capture 리스너

- **파일**: `src/edit/edit-manager.ts:1856-1890`, `layout-edit-controller.ts:846-853`, `text-edit-controller.ts:683-684`
- **심각도**: Medium
- **문제**: 다수 controller가 op 후 `_suppressLayoutClick()` 호출로 다음 click이 `LayoutSelectionController` 도달 차단. `stopPropagation()`이 synthetic click 방지 못하는 문제 회피. 200ms window fragile, pointer sequence ID 추적이 cleaner.

#### B3.11 _onBlur의 composition-end 로직 인라인 중복

- **파일**: `src/edit/text-edit-controller.ts:748-770`
- **심각도**: Low/Medium
- **문제**: `_onCompositionEnd`와 `_onBlur`에 동일 text-content sync + offset 계산 중복. `_onBlur`가 `_onCompositionEnd` 호출 불가(추가 이벤트 dispatch 방지). composition-end cursor positioning 수정 시 두 곳 유지 필요.

#### B3.12 EditManager._clearBoxSelectionForParagraph 의도적 no-op

- **파일**: `src/edit/edit-manager.ts:1057-1064`, `docs/EDITING_TEXT.md:674`
- **심각도**: Low
- **문제**: method body empty. comments/docs가 no-op 명시. paragraph blur 시 parent-box selection 유지용 placeholder. 요구사항 변경 시 누락 위험.

### 컴포넌트 (B3.13 ~ B3.18)

#### B3.13 model?.textContent ?? _sourceContent fallback

- **파일**: `src/components/layout/paragraph.element.ts:158`
- **심각도**: Medium
- **문제**: `data`/`content` setter가 `model.textContent`를 항상 갱신하지 않기에 존재. setter 미구현 갭 은폐.

#### B3.14 _rebuildingChildren / _pendingData flag

- **파일**: `src/components/layout/document.element.ts:101, 487-548`, `box.element.ts:94, 674-761`, `tr.element.ts:37`, `td.element.ts:73`, `table.element.ts:56`
- **심각도**: Low/Medium
- **문제**: `MutationObserver` callback과 external `data` read가 half-reconciled DOM 관찰 차단. atomic update 대신 defensive sync patch.

#### B3.15 _feedRgbaToEngine의 try/catch가 CORS taint 조용히 삼킴

- **파일**: `src/components/layout/image.element.ts:299-317`
- **심각도**: Low
- **문제**: catch block이 geometric overlap fallback, 이벤트/로그 없음. image/security 설정 문제 은폐.

#### B3.16 _ensureFallbackCellEngine()이 new TableCellEngine() 직접 사용

- **파일**: `src/components/layout/td.element.ts:481-491`
- **심각도**: Low
- **문제**: AGENTS.md `new` 금지(factory only) 위반. real table engine attach 전 stop-gap.

#### B3.17 빨간 inset shadow overflow 표시

- **파일**: `src/components/layout/paragraph.element.ts:257-260, 315-320`
- **심각도**: Low
- **문제**: `_hasOverflow` true 시 `:host`에 `inset 0 -8px 0 0 #ff0000` 적용. 직렬화 상태 아닌 시각적 band-aid. overflow 자체가 버그인지는 별도 검토 필요.

#### B3.18 optimistic/temporary span을 renderText() 시작 시 strip 후 재생성

- **파일**: `src/components/layout/column.element.ts:299-301`
- **심각도**: Low
- **문제**: `data-temporary="true"` span을 매 diff pass 시작 시 제거, `TextEditController`가 재생성. render path가 edit state를 자연 반영하지 못하는 패치.

---

## Part 4. 불필요/데드 루틴 (Dead/Unnecessary)

> 총 17건. export되었으나 호출자 없음, 중복 로직, 도달 불가 분기.

### 엔진 (D4.1 ~ D4.5)

#### D4.1 BoxEngine.findChildEngineById

- **파일**: `src/engine/box-engine.ts:289-291`
- **심각도**: Low
- **문제**: `codegraph_callers` 0건. `findBoxEngineById`만 사용됨. generic child lookup 의도였으나 DOM layer가 box 조회만 필요.

#### D4.2 Layout result types (ImageLayoutResult, BoxLayoutResult, TableLayoutResult, ParagraphLayoutResult, DocumentLayoutResult, LayoutResult)

- **파일**: `src/engine/types.ts:319-366`
- **심각도**: Low
- **문제**: `codegraph_callers` 0건. aspirational API surface. docs에 export로 명시되어 docs 관점에서는 dead 아님.

#### D4.3 engineMergeOverlapParts / EngineMmRect / EngineCursorPlacement alias

- **파일**: `src/index.ts:12-13`
- **심각도**: Low
- **문제**: 사용 0건. `@/types`/`@/utils`/`@/edit` 이름 충돌 해결용이나 실제 충돌 consumer 없음.

#### D4.4 DocumentEngine._buildParagraphEngine paragraph 재사용 경로

- **파일**: `src/engine/document-engine.ts:362-369`
- **심각도**: Low
- **문제**: 정상 `layout()` flow에서 `childEngines`가 scratch부터 재구축되어 도달 불가. DOM layer가 수동 설정 시에만 발생.

#### D4.5 ParagraphEngine._initLayoutMetrics 불필요 할당

- **파일**: `src/engine/paragraph-engine.ts:150-159`
- **심각도**: Low
- **문제**: `this._lineHeight = 0; this._lineHeight = fontSize * lineGap;`. 첫 할당 불필요.

### 편집 (D4.6 ~ D4.9)

#### D4.6 TextEditContextAdapter 전체 파일

- **파일**: `src/edit/text-edit-context-adapter.ts` 전체, `src/edit/index.ts:1`
- **심각도**: High (dead code이면서 docs가 활성 명시)
- **문제**: `codegraph_callers` 0건. `TextEditController`가 import/사용하지 않음. `updateBounds`, `onSelectionChange`, `EditContext` type declarations 미사용.
- **권장**: `TextEditController`에 통합 또는 파일 + export 제거.

#### D4.7 TextEditController._handleTripleClick

- **파일**: `src/edit/text-edit-controller.ts:113-115`
- **심각도**: Low
- **문제**: handler가 `void this._handleTripleClick`에 할당되나 event listener로 등록 안 됨. triple-click 미구현.
- **재현**: paragraph triple-click → browser native selection만, controller-managed selection 없음.

#### D4.8 TableKeyboardController.handleTab

- **파일**: `src/edit/table-keyboard-controller.ts:455-507`
- **심각도**: Medium
- **문제**: `handleKeyDown`이 `handleTab` 호출하지 않음, 도달 불가. public API이나 실제 호출 경로 없음.

#### D4.9 TextEditCoordinateMapper._lineEndPlacements

- **파일**: `src/edit/text-edit-coordinate-mapper.ts:64, 198-203`, `text-edit-controller.ts:2067-2085`
- **심각도**: Low
- **문제**: `_lineEndPlacements`가 모든 line end에 populate, `_updateCursorPosition`이 `getCursorPlacement(offset, true)` 사용 후 `_crossRightState`가 override. preferLineEnd path가 cross-state에 의해 shadowed.

### 컴포넌트 (D4.10 ~ D4.14)

#### D4.10 LayoutGuideColumnElement.disconnectedCallback 빈 본체

- **파일**: `src/components/layout/guide-column.element.ts:45-46`
- **심각도**: Low
- **문제**: override 존재하나 cleanup 없음.

#### D4.11 LayoutColumnElement.disconnectedCallback 빈 본체

- **파일**: `src/components/layout/column.element.ts:27-28`
- **심각도**: Low
- **문제**: override 존재하나 cleanup 없음.

#### D4.12 LayoutTableRowElement._layoutStructure 빈 본체

- **파일**: `src/components/layout/tr.element.ts:169-170`
- **심각도**: Low
- **문제**: pipeline decomposition 만족용, `table._layoutStructure`가 전담.

#### D4.13 LayoutTableElement.attributeChangedCallback / observedAttributes 빈

- **파일**: `src/components/layout/table.element.ts:146-155`
- **심각도**: Low
- **문제**: attribute handling 인프라만 존재, 응답 attribute 없음.

#### D4.14 box _appendChildData가 public appendChildData와 거의 동일

- **파일**: `src/components/layout/box.element.ts:789-855`
- **심각도**: Low
- **문제**: private variant가 data setter 내부만 사용, post-append notification만 차이. 중복.

### 리소스/유틸 (D4.15 ~ D4.17)

#### D4.15 src/utils/lru-cache.ts export LRU 사용처 없음

- **파일**: `src/utils/lru-cache.ts`, `src/utils/index.ts:5`
- **심각도**: Low
- **문제**: grep + codegraph_callers 0건. `ParagraphEngine`은 자체 private `_LRU` 사용. public `LRU`는 dead surface.

#### D4.16 ColorRegistry/FontLoader registerLoader/resetLoader 호출자 없음

- **파일**: `src/resource/color-registry.ts:19, 69-78`, `src/resource/font-loader.ts:21, 80-89`
- **심각도**: Low
- **문제**: public API hook이나 내부/React 호출자 없음. 미테스트.

#### D4.17 genRandom은 genUUID 내부에서만 사용

- **파일**: `src/utils/random.ts`, `src/utils/gen-uuid.ts:1`
- **심각도**: Low
- **문제**: `genRandom`은 `gen-uuid.ts`만 import, `index.ts`에서 re-export 안 됨. 무거운 환경 호환 코드가 단일 4-digit random을 위해 존재.

---

## Part 5. 미구현/불완전 (Incomplete Implementations)

> 총 12건. `as any`(FORBIDDEN), `@ts-ignore`, early return without work, stub, missing branch.

### 엔진 (I5.1 ~ I5.5)

#### I5.1 TableEngine printPostData 미구현

- **파일**: `src/engine/box-engine.ts:393-395`
- **심각도**: Medium
- **문제**: `// TableEngine의 printPostData는 향후 구현` 주석만. 순수 Node.js 사용자가 `engine.printPostData` 요청 시 table content 없음.

#### I5.2 ImageEngine.layout()이 objectFit 무시

- **파일**: `src/engine/image-engine.ts:433-444`
- **심각도**: Medium
- **문제**: `objectFit`/`originalWidth/Height` 기반 crop/display 미계산. raw `x/y/w/h` 통과. DOM layer가 별도 처리, 엔진 API는 미완성.

#### I5.3 ParagraphEngine.scale no-op

- **파일**: `src/engine/paragraph-engine.ts:1472-1480`
- **심각도**: Low
- **문제**: getter `1`, setter 무시. docs는 scale 정규화 명시.

#### I5.4 ParagraphEngine.getCursorPlacement(preferLineEnd) 미구현

- **파일**: `src/engine/paragraph-engine.ts:1337-1346`
- **심각도**: Medium
- **문제**: `preferLineEnd=true`가 줄 끝이 아닌 해당 char 우측 반환. DOM mapper의 `_lineEndPlacements` 로직과 불일치.

#### I5.5 GridCalculatorEngine explicit-column editableHeight floor 미적용

- **파일**: `src/engine/grid-calculator-engine.ts:121`
- **심각도**: Critical (C1.4와 동일)
- **문제**: docs가 명시한 lineHeight 정수배 내림 규칙 미적용.

### 편집 (I5.6 ~ I5.10)

#### I5.6 Triple-click paragraph 선택 미구현

- **파일**: `src/edit/text-edit-controller.ts:113-115`, `docs/EDITING_TEXT.md:119, 350-352`
- **심각도**: Medium
- **문제**: docs가 "triple-click 전체 선택" 지원 명시. source에 `_handleTripleClick` handler 존재하나 등록 안 됨.
- **재현**: paragraph triple-click → browser native selection만, controller-managed 선택 없음.

#### I5.7 cursorMove key repeat 패턴 미구현

- **파일**: `src/edit/text-edit-controller.ts:873-875`, `docs/EDITING_TEXT.md:380-397`
- **심각도**: Medium (C1.11과 동일)
- **문제**: `!event.repeat` 조건으로 반복 arrow가 0회 emit. keyup 이벤트 미계측. docs가 first-and-last 패턴 약속.

#### I5.8 EditContext API 통합 미완성

- **파일**: `src/edit/text-edit-context-adapter.ts:1-202`
- **심각도**: High (C1.10과 동일)
- **문제**: adapter가 `create`, `updateBounds`, `destroy`, event handler 구현하나 `TextEditController`가 인스턴스화하지 않음. `onSelectionChange` 정의되나 미사용.

#### I5.9 _onPaste가 line break / rich content 미처리

- **파일**: `src/edit/text-edit-controller.ts:1164-1198`
- **심각도**: Medium
- **문제**: `event.clipboardData.getData('text/plain')` 읽어 삽입. `\r\n`, tab, 초장문 처리 없음. `selectionStart`/`selectionEnd` notification ordering 없음.
- **재현**: Word에서 multi-line paste → `\r\n`이 extra blank line 생성.

#### I5.10 PlaceGunController style contentType 미완전

- **파일**: `src/edit/place-gun-controller.ts:159-160, 700-780`
- **심각도**: Medium
- **문제**: `handleBoxMouseDown`이 `_injectStylePattern`로 분기하나, target box에 paragraph 존재 여부 미검증. image-only box에 style item 소비 시 조용히 실패, item만 소진.
- **재현**: `style` Place Gun item load, image-only box click → 에러/효과 없이 item 소비.

### 컴포넌트 (I5.11 ~ I5.12)

#### I5.11 LayoutImageElement가 render-error 미발생

- **파일**: `src/components/layout/image.element.ts:361-365, 402-406`
- **심각도**: Medium
- **문제**: image load/draw 에러를 catch 후 조용히 실패, `render-error` 이벤트 없음. host가 broken/tainted image 감지 불가.
- **재현**: cross-origin image load → `drawImage` throw, image area cleared, 이벤트 없음.

#### I5.12 LayoutGuideColumnElement.printPostData가 engine-derived가 아님

- **파일**: `src/components/layout/guide-column.element.ts:179-190`
- **심각도**: Medium
- **문제**: docs/AGENTS.md는 engine mm × ppm 계약 명시. guide-column은 `getBoundingClientRect()` + `window.scrollX/Y` 사용. print mode에서 0 반환.

---

## Part 6. 문서-코드 불일치 (Doc/Code Drift)

> 총 20건. docs가 명시하는 동작을 코드가 구현하지 않거나, 반대로 코드가 docs에 없는 동작 수행.

### 엔진 docs (D6.1 ~ D6.5)

#### D6.1 docs/ENGINE.md §2.2 vs grid-calculator-engine.ts:121

- `editableHeight`가 "lineHeight의 정수배로 내림" 명시. explicit-column 분기가 위반.

#### D6.2 docs/ENGINE.md §2.5 vs image-engine.ts:433-444

- `ImageEngine.layout()`이 objectFit 기반 crop/display 반환 명시. 실제는 raw 통과.

#### D6.3 docs/ENGINE.md §2.1 예제

- `create` signature `(data, fontLoader, colorRegistry, ppm?)`이나 예제가 ppm을 두 번째 인자로 표기.

#### D6.4 docs/TEXT_ENGINE.md §2.5 vs paragraph-engine.ts:1393

- `textContent` getter 타입이 docs는 `string`, 실제는 `string | (string|TextBlockData)[]`.

#### D6.5 docs/PERFORMANCE.md §2.2 vs paragraph-engine.ts:1093

- `_charOuterStyleCache` key가 docs는 `${char}|${widthRatio}|${letterSpacing}|${spaceRatio}`, 실제는 `|${fs}`(fontSize) 추가 포함.

### 편집 docs (D6.6 ~ D6.10)

#### D6.6 EditManager singleton vs per-document

- `docs/EDITING_EVENTS.md:111`, `docs/EDITING_TEXT.md:456-458`, `docs/EDITING_LAYOUT.md:653`가 "글로벌 편집 관리 싱글톤" 명시. `AGENTS.md`와 code(`new EditManager(this)`)는 per-document 인스턴스.

#### D6.7 EditContext API 연결 거짓 명시

- `docs/EDITING_TEXT.md:21-24, 208-230`이 EditContext를 `TextEditController` 대체 입력 경로라 명시. 실제 연결 없음.

#### D6.8 handleTab 경로 거짓 명시

- `docs/EDITING_TABLE.md:371-389`가 `TableKeyboardController.handleTab`을 table 내부 paragraph focus 경로라 명시. 실제 `document.element.ts`가 Tab을 window capture에서 `navigateByTab` 호출, table-internal Tab 불가.

#### D6.9 이벤트 개수 불일치

- `docs/EDITING_EVENTS.md:11`이 "16가지 이벤트 타입" 명시. `EditManagerEventType` union은 20개 항목(`placeGunBefore`, `placeGunAfter`, `cellSelectionChange`, `contextMenu`, `boxPropertyChange` 등 포함).

#### D6.10 snapEnabled 미존재

- `src/edit/layout-edit-controller.ts:20` 주석이 `EditManager.snapEnabled` 언급. 실제 getter/setter 없음.

### 컴포넌트 docs (D6.11 ~ D6.18)

#### D6.11 ParagraphEngine.scale 할당 미구현

- `docs/TEXT_ENGINE.md §12.5.1, §13.4` vs `paragraph.element.ts:292-373`. 할당 명시, 미구현.

#### D6.12 objectFit이 렌더링에 사용되나 docs는 UI 메타데이터만 명시

- `docs/API.md §Image objectFit` vs `image.element.ts:442-461`. docs는 "편집 UI 메타데이터 ONLY, LayoutImageElement 렌더링 미사용". 코드는 `computeObjectFit()` 호출로 crop 좌표 도출.

#### D6.13 objectFit setter trigger 불일치

- `docs/API.md §Image triggers` vs `image.element.ts:627-632`. docs는 `(없음)`, 코드는 `_applyObjectFit(); this.render()`.

#### D6.14 Box contentType에 'table' 누락

- `docs/API.md §Box contentType` vs `box.element.ts:1332`. docs는 `'image' | 'paragraph' | null`, 실제는 `'table'` 포함.

#### D6.15 guide-column printPostData가 getBoundingClientRect 사용

- `docs/API.md §printPostData`, `AGENTS.md §print mode` vs `guide-column.element.ts:179-190`. engine mm×ppm 계약 명시, 실제 `getBoundingClientRect()` 사용.

#### D6.16 paragraph.data setter가 full re-creation 미수행

- `docs/API.md §Paragraph data setter` vs `paragraph.element.ts:481-495`. docs는 full re-creation, 코드는 `layout()` + flag만.

#### D6.17 paragraph.content setter가 TextBlockData[] 미지원

- `docs/API.md §Paragraph content setter` vs `paragraph.element.ts:559-563`. docs는 `TextBlockData[]` 타입 허용, 코드는 string만 처리.

#### D6.18 Host z-index +100 offset이 예약 범위 위반

- `AGENTS.md §z-index range` vs `box.element.ts:452` 외. 0~90000 레이아웃, 90001~99999 UI 예약 명시. `+100`이 일반 값도 90001+로 밀어넣음.

### 리소스 docs (D6.19 ~ D6.20)

#### D6.19 screen mode ttfFilename vs base64Data 우선

- `docs/RESOURCE.md` vs `font-loader.ts:147-150`. docs는 screen mode가 `ttfFilename` 사용, 코드는 `base64Data` 우선(양 모드). AGENTS.md는 정확하나 standalone resource doc은 stale 가능.

#### D6.20 _defaultColor 초기값 불일치

- `docs/RESOURCE.md:335, 343, 370` vs `color-registry.ts:139`. docs가 table에서 `{c:0,m:0,y:0,k:0}`, sequence diagram에서 `{c:0,m:0,y:0,k:255}`. 코드는 init 후 `{c:0,m:0,y:0,k:255}`. table이 잘못됨.

---

## Part 7. 통합 계층 특화 (Engine↔DOM Seam)

> 총 8건. 엔진과 DOM 양쪽의 불일치에서 발생하는 seam 버그. 각 항목은 엔진 file:line과 DOM file:line 양쪽 인용.

### S1 FontLoaderSingletonAdapter의 as unknown as ParsedFont 캐스트

- **엔진**: `src/engine/types.ts:190-198` (`ParsedFont` 요구 `charToGlyph(char): { advanceWidth: number }`)
- **DOM**: `src/components/layout/document.element.ts:34-35` (`this._fl.getParsedFont(fontName) as unknown as ParsedFont | null`)
- **심각도**: Medium
- **문제**: 캐스트가 browser-side `opentype.Font`가 엔진으로 feed되는 경계에서 type system 우회. opentype.js가 `charToGlyph` return shape 변경 시(예: `advanceWidth` nullable) 컴파일 타임 에러 없이 런타임 실패.
- **재현**: opentype.js upgrade where `Glyph.advanceWidth` optional → cast still compiles, `ParagraphEngine._charWidthMmFromFont`가 undefined 읽어 NaN width.

### S2 Image engine이 URL 변경 후 stale rgbaData 보유

- **엔진**: `src/engine/image-engine.ts:99-101` (`set rgbaData` overwrite only)
- **DOM**: `src/components/layout/image.element.ts:413-418` (`_clearImageCache`는 JS image/URL cache만), `:479-487` (`set data`가 `urlChanged` 시 `_clearImageCache()` 호출)
- **심각도**: High
- **문제**: URL 변경과 새 image `load` 이벤트 사이에 engine이 이전 image pixel mask 보유. 중간 reflow 시 이전 이미지 shape으로 wrapping.
- **재현**: image A load, `image.data = { ..., url: 'B.png' }`, 즉시 paragraph resize → A의 rgbaData로 wrapping.

### S3 Image engine data가 inheritStyle/좌표 setter 후 미갱신

- **엔진**: `src/engine/image-engine.ts:76-83` (`set data`만 `_data` 갱신)
- **DOM**: `src/components/layout/image.element.ts:528-550` (x/y/width/height/dpi setter가 `render()`만), `:717-722` (inheritStyle setter도 `_updateEngine()` 미호출)
- **심각도**: High
- **문제**: DOM은 crop/rect 재계산, engine은 stale geometry. overlap/printPostData가 old x/y/w/h 사용.
- **재현**: `objectFit: 'cover'`, `originalWidth/Height` 설정 image, 부모 box resize → DOM은 올바른 crop, sibling paragraph는 stale engine geometry로 wrapping.

### S4 ImageData 좌표 단위가 docs(source pixels) vs 코드(mm) 불일치

- **엔진**: `src/engine/image-engine.ts:433-444` (x/y/w/h를 mm로 직접 사용, dpi 변환 없음)
- **DOM**: `src/components/layout/image.element.ts:383-407` (`_drawImage`도 mm로 처리)
- **심각도**: Medium
- **문제**: AGENTS.md는 `objectFit !== 'none'` 시 x/y/w/h가 **source image pixels**이고 dpi로 mm 변환 명시. 엔진/DOM 모두 mm. docs 위반.
- **재현**: print mode에서 `objectFit: 'cover'`, x/y/w/h를 source pixels로 전달 → `printPostData`가 pixels을 mm로 보고.

### S5 ParagraphEngine 캐시 해시가 rgbaData/overlapPadding 미포함

- **엔진**: `src/engine/paragraph-engine.ts:882-924` (해시), `:603-607` (캐시 히트)
- **DOM**: `src/components/layout/image.element.ts:325-329` (`_notifyOverlapParagraphs`), `paragraph.element.ts:292-311` (`render()`가 `_layoutStructure` + `layoutText`)
- **심각도**: High (C1.7과 동일)
- **문제**: 해시가 overlay 위치/크기/overlapMode 포함, rgbaData 존재 여부와 overlapPadding 미포함. 이미지 로드 후 rgbaData 주입 시 해시 동일 → 캐시 히트 → 기하학적 fallback 재사용.
- **재현**: path mode image over paragraph, 이미지 로드 전 렌더링(geometric fallback), 로드 후 `_notifyOverlapParagraphs` 트리거 → 해시 동일 → 픽셀 wrapping 미수행.

### S6 overlapMode: 'none' paragraph가 multi-child group box 내부 시 필터 스킵

- **엔진**: `src/engine/box-engine.ts:271-282` (`contentType === 'paragraph'`일 때만 필터)
- **DOM**: `src/components/layout/box.element.ts:1252-1263` (동일)
- **심각도**: Medium (P2.32와 동일)
- **문제**: multi-child group box `contentType`이 `null` → 필터 스킵 → byline이 overlay로 작동.

### S7 편집 중 data 재할당이 model.textContent 덮어쓰기

- **엔진**: `src/engine/paragraph-engine.ts:1393-1395` (`set textContent` 단순 대체)
- **DOM**: `src/components/layout/paragraph.element.ts:481-495` (`set data`가 `this._model.textContent = data.content`)
- **심각도**: High (P2.33과 동일)
- **문제**: 외부 `data` 재할당 시 편집 내용 소실.

### S8 빈 table cell에 box 추가 시 첫 layout tick까지 engine orphan

- **엔진**: `src/engine/table-engine.ts:280-292` (`cellChildren.length === 0` 시 `cellEngine.boxEngine` null)
- **DOM**: `src/components/layout/box.element.ts:285-294` (`_findParentEngine`), `td.element.ts:748-754` (`appendChildData`)
- **심각도**: Medium (P2.34와 동일)
- **문제**: 빈 cell box 추가 시 첫 layout까지 orphan.

---

## Appendix A. 엔진 계층 상세 보고서

> 본 부록은 `src/engine/` 감사 에이전트의 원본 보고서를 그대로 보존. 위 본문과 일부 중복 가능.

### A.1 Critical Bugs

#### A.1.1 GridCalculatorEngine explicit-column editableHeight floor 생략
- **File**: `src/engine/grid-calculator-engine.ts:121`
- numeric-column 분기(line 102)는 `Math.floor((height - padding) / lineHeight) * lineHeight` 사용. explicit-column(`columns: number[]`) 분기는 `height - padding` 그대로. fractional 마지막 줄 → overflow 임계값 divergence.
- **Reproduction**: `columns: [40,40,40]`, `lineHeight=4.8`, `height=370` → `editableHeight=370` vs equal-column `369.6`.

#### A.1.2 BoxEngine.absHeight static clamp padding 이중 차감
- **File**: `src/engine/box-engine.ts:208-210`
- `relTop`이 부모 grid paddingTop 포함, `_data.paddingTop`은 자식 자체 패딩. 이중 차감 → 음수 → 0 → 박스 붕괴.

#### A.1.3 DocumentEngine._buildBoxEngine reparent 시 엔진 이중 부모
- **File**: `src/engine/document-engine.ts:269`
- `existingBox.parent = parent`가 기존 부모 `childEngines`에서 제거 없이 새 부모 할당.

#### A.1.4 ParagraphEngine 레이아웃 캐시 stale _overlayRectsMm
- **File**: `src/engine/paragraph-engine.ts:603-607`
- 캐시 히트 시 `_overlayRectsMm = null` 할당 후 early return. `_detectOverlapWithCache` 재호출 없음. rgbaData 후속 주입 시 해시 동일 → stale 결과.

#### A.1.5 ParagraphEngine.getCharRect stripped space 인덱스 오류
- **File**: `src/engine/paragraph-engine.ts:1214-1226`
- `charOffsets[localIdx]`를 raw content 인덱스로 조회. stripping 시 mis-align.

#### A.1.6 ParagraphEngine._computeLayoutInputHash overlapPadding 미포함
- **File**: `src/engine/paragraph-engine.ts:882-924`
- overlapPadding 변경 시 해시 동일 → stale layout.

#### A.1.7 overlap-engine / image-engine 0-width 0으로 나눔
- **File**: `src/engine/overlap-engine.ts:140`, `src/engine/image-engine.ts:208`
- `scaleX = rgba.width / imgRectMm.width`. 0 width → Infinity → NaN.

#### A.1.8 border-resolver key parsing fragile
- **File**: `src/engine/border-resolver.ts:269-272`
- `key.split('-')`가 `dir-index-col` 형식 가정. 추가 dash 지원 시 파싱 오류. 내부 edge 필터 로직에 comment 없음.

### A.2 Potential/Latent Bugs

| #       | File:line                         | Issue                                                                                                  |
| ------- | --------------------------------- | ------------------------------------------------------------------------------------------------------ |
| A.2.1   | `box-engine.ts:230-241`             | `contentType` 혼합 자식에서 'table' 우선                                                                |
| A.2.2   | `box-engine.ts:262-287`             | `overlayElements` 부모 overlay 무조건 추가, 교집합 필터 없음                                            |
| A.2.3   | `grid-calculator-engine.ts:101-103` | 음수 editableWidth 미검증                                                                                |
| A.2.4   | `paragraph-engine.ts:810-815`       | force-place part width 초과 허용                                                                          |
| A.2.5   | `paragraph-engine.ts:342-349`       | stripped space와 global offset 불일치                                                                    |
| A.2.6   | `table-grid-resolver.ts:99-127`     | `normalizeWidths` 경계 케이스                                                                             |
| A.2.7   | `image-engine.ts:433-444`           | `ImageEngine.layout()` objectFit 무시                                                                   |
| A.2.8   | `document-engine.ts:83-86`          | `data` setter가 `childBoxEngines` 재구축 안 함                                                            |
| A.2.9   | `paragraph-engine.ts:1264`          | `getOffsetFromPoint` 마지막 column 우측 `null`                                                            |
| A.2.10  | `paragraph-engine.ts:1337-1346`     | `getCursorPlacement(preferLineEnd)` 미구현                                                              |
| A.2.11  | `paragraph-engine.ts:1543`          | `_LRU` `Map.keys().next()` 구형 타겟                                                                     |
| A.2.12  | `document-engine.ts:240-259`        | `_refreshParagraphOverlays` inheritStyle 미재계산                                                        |

### A.3 Band-Aid/Patch-Up

- **A.3.1** `paragraph-engine.ts:810-821` — force-place 무한루프 가드.
- **A.3.2** `overlap-engine.ts:114-119`, `image-engine.ts:173-180` — rgbaData 미제공 시 geometric fallback.
- **A.3.3** `paragraph-engine.ts:518, 698, 727, 812, 851` 외 — `1e-6` epsilon.
- **A.3.4** `paragraph-engine.ts:537-538` — `minCharWidthMm` 미만 free region 폐기.
- **A.3.5** `paragraph-engine.ts:1472-1480` — `scale` no-op.
- **A.3.6** `paragraph-engine.ts:573-575` — `_initStructureAndMeasureColumns` 본체 없음.
- **A.3.7** `color-registry-engine.ts:27, 52` — `_defaultColor` 이중 초기화.
- **A.3.8** `font-loader-engine.ts:97-99` — parse 실패 조용히 스킵.

### A.4 Dead/Unnecessary

- **A.4.1** `box-engine.ts:289-291` — `findChildEngineById` 호출자 0건.
- **A.4.2** `engine/types.ts:319-366` — Layout result types 인스턴스화 0건.
- **A.4.3** `src/index.ts:12-13` — `engineMergeOverlapParts`, `EngineMmRect`, `EngineCursorPlacement` alias 사용 0건.
- **A.4.4** `document-engine.ts:362-369` — paragraph 재사용 경로 도달 불가.
- **A.4.5** `paragraph-engine.ts:150-159` — `_initLayoutMetrics` 불필요 `this._lineHeight = 0` 할당.

### A.5 Incomplete Implementations

- **A.5.1** `box-engine.ts:393-395` — `TableEngine` `printPostData` 미구현.
- **A.5.2** `image-engine.ts:433-444` — `ImageEngine.layout()` objectFit 미구현.
- **A.5.3** `paragraph-engine.ts:1472-1480` — `scale` no-op.
- **A.5.4** `paragraph-engine.ts:1337-1346` — `getCursorPlacement(preferLineEnd)` 미구현.
- **A.5.5** `grid-calculator-engine.ts:121` — explicit-column `editableHeight` floor 미적용.

### A.6 Doc/Code Drift

- **A.6.1** `docs/ENGINE.md §2.2` vs `grid-calculator-engine.ts:121` — `editableHeight` 정수배 내림 위반.
- **A.6.2** `docs/ENGINE.md §2.5` vs `image-engine.ts:433-444` — `ImageEngine.layout()` objectFit 미구현.
- **A.6.3** `docs/ENGINE.md §2.1 예제` — `create` signature 예제 ppm 위치 오기.
- **A.6.4** `docs/TEXT_ENGINE.md §2.5` vs `paragraph-engine.ts:1393` — `textContent` getter 타입 불일치.
- **A.6.5** `docs/PERFORMANCE.md §2.2` vs `paragraph-engine.ts:1093` — `_charOuterStyleCache` key에 `fontSize` 누락.

---

## Appendix B. 편집 계층 상세 보고서

> 본 부록은 `src/edit/` 감사 에이전트의 원본 보고서를 그대로 보존.

### B.1 Critical Bugs

#### B.1.1 TextEditContextAdapter 미인스턴스화
- **Files**: `src/edit/text-edit-context-adapter.ts:60-72`, `src/edit/text-edit-controller.ts:80-115`
- **Severity**: Critical
- `codegraph_callers` 0건. `TextEditController`가 `TextEditContextAdapter.create()` 결코 호출하지 않음. EditContext API가 dead code.
- **Impact**: docs가 EditContext 사용 명시, 실제는 textarea fallback. IME 좌표 reporting 등 미해결.

#### B.1.2 cursorMove key repeat 중 0회 발생
- **Files**: `src/edit/text-edit-controller.ts:873-875`, `docs/EDITING_TEXT.md:380-397`
- **Severity**: Critical
- `!event.repeat && isCursorKey` 조건. 반복 arrow에서 `cursorMove` 0회. keyup 미계측. docs가 first-and-last 약속 위반.
- **Impact**: 외부 UI 커서 위치 표시기 stale.

#### B.1.3 IME Escape 미롤백
- **Files**: `src/edit/text-edit-controller.ts:774-783`
- **Severity**: Critical
- composition 중 Escape가 `_isComposing = false`, composition span 제거, 커서 재위치. `model.textContent`/`textarea.value` 복원 없음. `_onCompositionEnd` 트리거 안 됨.
- **Impact**: 미완성 조합 문자 문서 누출.

#### B.1.4 LayoutEditController reparent 상태 미초기화
- **Files**: `src/edit/layout-edit-controller.ts:794-905`
- **Severity**: Critical
- `state.reparentOutside`가 mouseup 후 reset 안 됨. 동일 박스 재드래그 시 stale 값 사용.
- **Impact**: 재드래그 좌표 점프.

#### B.1.5 _suppressLayoutClick 타이머 < dblclick 임계값
- **Files**: `src/edit/edit-manager.ts:1856-1875`
- **Severity**: Critical
- 200ms suppression vs ~300ms dblclick. 두 번째 click이 suppression 소멸 후 도달.
- **Impact**: selection 불일치.

#### B.1.6 InsertController mousedown bubbling phase vs layout-edit capture
- **Files**: `src/edit/insert-controller.ts:43, 73-79`, `src/edit/layout-edit-controller.ts:420-500`
- **Severity**: Critical
- `InsertController`가 `mousedown`을 bubbling phase로 document에 attach. `LayoutEditController`가 capture phase에서 `stopPropagation()` → bubble-phase insert drag 차단.
- **Impact**: insert mode 빈 공간 drag가 box 근처에서 불안정.

### B.2 Potential/Latent Bugs

| #       | File:line                              | Issue                                                                                                       |
| ------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| B.2.1   | `text-edit-coordinate-mapper.ts:758-775` | `_findColumnIndexByOffset` 마지막 column `null`                                                                |
| B.2.2   | `text-edit-coordinate-mapper.ts:41`      | `useEngineCoordinateQueries` 영구 false                                                                          |
| B.2.3   | `text-edit-coordinate-mapper.ts:67`      | `_columnSpansCache` 무효화 누락                                                                                  |
| B.2.4   | `text-edit-controller.ts:570-635`        | `_onMouseDown` event.button 미검사                                                                              |
| B.2.5   | `edit-manager.ts:340-354`                | `reset()`이 `_selectionController` detach 안 함                                                                  |
| B.2.6   | `layout-edit-controller.ts:541-545`      | Alt-drag clone `source: 'insert'`                                                                              |
| B.2.7   | `table-keyboard-controller.ts:455-507`   | `handleTab` 도달 불가                                                                                            |

### B.3 Band-Aid/Patch-Up

- **B.3.1** `text-edit-controller.ts:74, 84, 391-395, 2025-2029` — `_wasFocused`/`_pendingTextChangeOnBlur` flag.
- **B.3.2** `edit-manager.ts:1856-1890` — `_suppressLayoutClick` window capture.
- **B.3.3** `text-edit-controller.ts:748-770` — `_onBlur` composition-end 로직 인라인 중복.
- **B.3.4** `edit-manager.ts:1057-1064` — `_clearBoxSelectionForParagraph` no-op.
- **B.3.5** `edit-manager.ts:1909-1945`, `layout-edit-controller.ts:636-650` — `getTopLevelDragTargets()` 이중 필터.

### B.4 Dead/Unnecessary

- **B.4.1** `text-edit-context-adapter.ts` 전체 — 호출자 0건.
- **B.4.2** `text-edit-coordinate-mapper.ts:71-73, 758-775` — `_columnRanges` binary search 중복.
- **B.4.3** `text-edit-controller.ts:113-115` — `_handleTripleClick` listener 미등록.
- **B.4.4** `table-keyboard-controller.ts:455-507` — `handleTab` 도달 불가.
- **B.4.5** `text-edit-coordinate-mapper.ts:64, 198-203` — `_lineEndPlacements` cross-state override.
- **B.4.6** `text-edit-controller.ts:433-439`, `edit-manager.ts:533-535` — `_consumePendingTextChange` inline 가능.

### B.5 Incomplete Implementations

- **B.5.1** `text-edit-controller.ts:113-115` — triple-click 미구현.
- **B.5.2** `text-edit-controller.ts:873-875` — `cursorMove` key repeat 미구현.
- **B.5.3** `text-edit-context-adapter.ts:1-202` — EditContext API 미완성.
- **B.5.4** `text-edit-controller.ts:1164-1198` — `_onPaste` line break/rich content 미처리.
- **B.5.5** `place-gun-controller.ts:159-160, 700-780` — `style` contentType 미완전.

### B.6 Doc/Code Drift

- **B.6.1** `docs/EDITING_EVENTS.md:111` 외 — singleton vs per-document.
- **B.6.2** `selectionChange` 참고: 이미 docs에서 제거됨, 정정 완료.
- **B.6.3** `docs/EDITING_TEXT.md:21-24, 208-230` — EditContext 연결 거짓 명시.
- **B.6.4** `docs/EDITING_TABLE.md:371-389` — `handleTab` 경로 거짓 명시.
- **B.6.5** `docs/EDITING_LAYOUT.md:235-290` — `selectableRoles` fallback (대부분 정확, 미묘함).
- **B.6.6** `src/edit/layout-edit-controller.ts:20` — `snapEnabled` 미존재.
- **B.6.7** `docs/EDITING_EVENTS.md:11` — 16가지 vs 20가지 이벤트.
- **B.6.8** `docs/EDITING_INSERT.md`, `EDITING_PLACE_GUN.md` — `_suppressLayoutClick` caller 목록 불완전.

---

## Appendix C. 컴포넌트 계층 상세 보고서

> 본 부록은 `src/components/` 감사 에이전트의 원본 보고서를 그대로 보존.

### C.1 Critical Bugs

#### C.1.1 ParagraphEngine.scale 미할당
- **File**: `src/components/layout/paragraph.element.ts:292-373`
- `TEXT_ENGINE.md §12.5.1, §13.4`가 `editManager.scale` → `model.scale` 할당 명시. 코드 미구현.
- **Reproduction**: `setScale(2)` 후 box resize → reflow 위치 drift.

#### C.1.2 Stale ParagraphEngine/ImageEngine 재사용
- **File**: `src/components/layout/paragraph.element.ts:133-135`, `image.element.ts:500-501`
- `find(e => e instanceof ParagraphEngine/ImageEngine)`가 owner 미검증. 제거 후 새 요소가 이전 엔진 재사용. `disconnectedCallback`이 `childEngines`에서 제거 안 함.

#### C.1.3 paragraph.data setter render 미스케줄
- **File**: `src/components/layout/paragraph.element.ts:481-495`
- `layout()` + flag만, `scheduleRender()` 미호출. 직접 `paragraph.data = {...}` 시 DOM stale.

#### C.1.4 paragraph.content setter TextBlockData[] 무시
- **File**: `src/components/layout/paragraph.element.ts:559-563`
- string만 `model.textContent` 갱신, `TextBlockData[]`은 `_sourceContent`만. stale model 승리.

#### C.1.5 visibleGuide setter 미저장
- **File**: `src/components/layout/document.element.ts:658-667`
- `this._visibleGuide = value` 미할당. 다음 `layout()` 시 stale 값으로 guide 재생성.

#### C.1.6 Host z-index +100 offset
- **File**: `src/components/layout/box.element.ts:452`, `paragraph.element.ts:256`, `image.element.ts:211`
- `z-index: ${this.zIndex + 100}`. role ad/header이 91100/91101 → UI chrome 범위 침범.

#### C.1.7 guide-column printPostData가 getBoundingClientRect 사용
- **File**: `src/components/layout/guide-column.element.ts:179-190`
- docs는 engine mm×ppm 명시. 코드는 `getBoundingClientRect()` + scroll. print mode에서 0.

#### C.1.8 Table row resize Escape _rows 미복원
- **File**: `src/components/layout/table.element.ts:710-731`
- `_applyRowResize()`가 `this._rows[topIdx].height` 변형. Escape가 TR height만 복원, `_rows` 미복원. 다음 layout 시 변형된 높이 복귀.

#### C.1.9 contentElement가 table content 잘못 캐스트
- **File**: `src/components/layout/box.element.ts:1352-1368`
- ParagraphEngine/ImageEngine만 분기. table content 시 table 요소가 `LayoutImageElement | LayoutParagraphElement`로 캐스트. overlap 수집 시 `overlapMode` undefined.

#### C.1.10 Static box in TD single-column grid crash
- **File**: `src/components/layout/box.element.ts:232-251, 263-264`
- TD 내 static box가 `columns: [tdContentWidth]`로 single-column grid. `left > 0` 또는 `width > 1` 시 `columnWidth.slice(this.left, this.left + this.width)`가 `[]` → `absWidth = 0` 또는 `undefined` access.

#### C.1.11 LayoutImageElement.render()가 drawImage 실패 조용히 삼킴
- **File**: `src/components/layout/image.element.ts:402-406`
- `ctx.drawImage` try/catch가 silent return. CORS taint/invalid image 시 blank canvas, `render-error` 없음.

### C.2 Potential/Latent Bugs

| #       | File:line                   | Issue                                                                                                       |
| ------- | --------------------------- | ----------------------------------------------------------------------------------------------------------- |
| C.2.1   | `paragraph.element.ts:133-135`, `image.element.ts:500-501` | 다중 paragraph/image 시 단일 엔진 공유                                                                          |
| C.2.2   | `box.element.ts:737-739` 외     | data setter live HTMLCollection 인덱스 비교                                                                    |
| C.2.3   | `box.element.ts:1773-1800`    | overlayElements AABB getBoundingClientRect, scale 분할 없음                                                   |
| C.2.4   | `document.element.ts:211-221` | `_onWindowKeyDown` 모든 table Alt+Arrow 차단                                                                    |
| C.2.5   | `box.element.ts:674-761`       | data setter unconditional layout/render                                                                       |
| C.2.6   | `column.element.ts:314-335`    | overflow 라인 display:none span 생성 비용                                                                      |
| C.2.7   | `td.element.ts:698-728`        | placeholder border 빈 _cellLabels row 0/col 0 처리                                                            |
| C.2.8   | `table.element.ts:332-356`     | `_layoutStructure` children[r] 순서 미검증                                                                      |
| C.2.9   | `table.element.ts:258-260`     | cell selection overlay layout() 시만 갱신                                                                       |
| C.2.10  | `image.element.ts:467`         | `data` setter overlapMode 'path' reset                                                                          |
| C.2.11  | `box.element.ts:452` 외         | z-index +100이 90000 초과 가능                                                                                  |
| C.2.12  | `image.element.ts:387-395`     | `_drawImage` canvas Math.round sub-pixel 스킵                                                                   |

### C.3 Band-Aid/Patch-Up

- **C.3.1** literal TODO/FIXME 없음.
- **C.3.2** `paragraph.element.ts:775` — `const self: any = this;` cast.
- **C.3.3** `paragraph.element.ts:158` — `model?.textContent ?? _sourceContent` fallback.
- **C.3.4** `document.element.ts:101, 487-548` 외 — `_rebuildingChildren`/`_pendingData` flag.
- **C.3.5** `image.element.ts:299-317` — `_feedRgbaToEngine` try/catch CORS taint 삼킴.
- **C.3.6** 다수 파일 — `if (!this.isConnected)` early return.
- **C.3.7** `td.element.ts:481-491` — `_ensureFallbackCellEngine()` `new TableCellEngine()` 직접.
- **C.3.8** `paragraph.element.ts:257-260` — 빨간 inset shadow overflow 표시.
- **C.3.9** `column.element.ts:299-301` — optimistic span strip 후 재생성.
- **C.3.10** `box.element.ts:432-439` — static box in TD editableWidth/Height로 sizing, gap 발생.
- **C.3.11** `document.element.ts:199-232` — F5/Alt+Arrow hard-coded interception.
- **C.3.12** `box.element.ts:1610-1630` — `_absoluteToStaticCoords` Math.floor/Math.max(0) clamp.

### C.4 Dead/Unnecessary

- **C.4.1** `guide-column.element.ts:45-46` — `disconnectedCallback` 빈 본체.
- **C.4.2** `column.element.ts:27-28` — `disconnectedCallback` 빈 본체.
- **C.4.3** `tr.element.ts:169-170` — `_layoutStructure` 빈 본체.
- **C.4.4** `paragraph.element.ts:269-271` — `_propagateInheritStyle` 빈 본체.
- **C.4.5** `table.element.ts:146-155` — `attributeChangedCallback`/`observedAttributes` 빈.
- **C.4.6** `table.element.ts:502-511` — `_notifyTablePropertyChange` thin wrapper, `as unknown` cast.
- **C.4.7** `box.element.ts:537-546, 574-582` — `_renderBorder` 중복 border 제거 분기.
- **C.4.8** `box.element.ts:789-855` — private `_appendChildData` vs public `appendChildData` 중복.
- **C.4.9** `box.element.ts:1734-1739` — `_collectAffectedParagraphs` tableChild 이중 loop.
- **C.4.10** `document.element.ts:669-683` — `printPostData`가 print mode에서도 guide column 조회.

### C.5 Incomplete Implementations

- **C.5.1** `box.element.ts:1352-1368` — `contentElement` table content 미처리.
- **C.5.2** `image.element.ts:442-461` — `objectFit`이 API.md "UI 메타데이터 ONLY" 명시에도 렌더링에 사용.
- **C.5.3** `paragraph.element.ts:481-495` — `data` setter render 미스케줄.
- **C.5.4** `paragraph.element.ts:559-563` — `content` setter `TextBlockData[]` 미지원.
- **C.5.5** `td.element.ts:614-624` — placeholder border가 host attribute 아닌 manager property 사용.
- **C.5.6** `image.element.ts:361-365, 402-406` — image load/draw 실패 시 `render-error` 없음.
- **C.5.7** `guide-column.element.ts:179-190` — `printPostData` engine-derived 아님.
- **C.5.8** `box.element.ts:452` 외 — z-index +100 offset 미문서화.
- **C.5.9** `box.element.ts:1066` — `data` getter `role: this._role`(undefined for 'none') → 직렬화 시 `role` 필드 누락.
- **C.5.10** `box.element.ts:909-915` — `position` setter가 `requestRerenderAffectedParagraphs()` 미호출.
- **C.5.11** `box.element.ts:117-119` — `connectedCallback`가 `showPlaceholderBorders` 1회 읽기, toggle 미반영.

### C.6 Doc/Code Drift

- **C.6.1** `docs/TEXT_ENGINE.md §12.5.1, §13.4` vs `paragraph.element.ts:292-373` — `ParagraphEngine.scale` 할당 명시, 미구현.
- **C.6.2** `docs/API.md §Image objectFit` vs `image.element.ts:442-461` — `objectFit` "UI 메타데이터 ONLY" 명시, 실제 렌더링 사용.
- **C.6.3** `docs/API.md §Image triggers` vs `image.element.ts:627-632` — `objectFit` setter trigger `(없음)` 명시, 실제 `_applyObjectFit(); render()`.
- **C.6.4** `docs/API.md §Box contentType` vs `box.element.ts:1332` — `'table'` 누락.
- **C.6.5** `docs/API.md §printPostData`, `AGENTS.md` vs `guide-column.element.ts:179-190` — engine mm×ppm 위반.
- **C.6.6** `docs/API.md §Paragraph data setter` vs `paragraph.element.ts:481-495` — full re-creation 명시, 미구현.
- **C.6.7** `docs/API.md §Paragraph content setter` vs `paragraph.element.ts:559-563` — `TextBlockData[]` 미지원.
- **C.6.8** `AGENTS.md §z-index range` vs `box.element.ts:452` 외 — +100 offset 위반.
- **C.6.9** `AGENTS.md` `setBoxLockDeep` — components가 아닌 editing-controller routine.
- **C.6.10** `docs/API.md §Image originalWidth/originalHeight` vs `image.element.ts:642-662` — docs가 Place Gun injection 명시, 코드가 objectFit crop에 사용.

---

## Appendix D. 리소스/유틸/타입 계층 상세 보고서

> 본 부록은 `src/resource/`, `src/utils/`, `src/types/` 감사 에이전트의 원본 보고서를 그대로 보존.

### D.1 Critical Bugs

#### D.1.1 ColorRegistry._cmykToRgb 공식 오류
- **File**: `src/resource/color-registry.ts:104-119`
- **Severity**: Critical
- 표준 `r = round(255 * (1 - c/100) * (1 - k/100))` 대신 `r = round(255 * (1 - min(1, c + k)))` 사용. 중간 톤 + 검은 잉크가 검은색으로 붕괴.
- **Reproduction**: `{c:128, m:0, y:0, k:128}` → `#000000`, 정답 ~`#404040`.
- **Docs drift**: `docs/RESOURCE.md`가 공식 미명시, 코드가 일반 인쇄 기대와 상이.

#### D.1.2 ColorRegistry.get('default') 잘못된 기본값
- **File**: `src/resource/color-registry.ts:209-211`
- **Severity**: Critical
- `get(name)`이 `_colorSet[name] || _defaultColor` 반환. `_defaultColor` init 후 `{c:0,m:0,y:0,k:255}`(검은). `get('default')`가 등록된 색이 아닌 `_defaultColor` 반환 가능.
- **Reproduction**: `{ default: {c:0,m:0,y:0,k:0} }` 등록 후 `registry.get('default')` → `_defaultColor`(검은) 반환. `getCSSColor('default')`는 등록된 색(흰, 공식 오류로) 반환. API surface 불일치.

#### D.1.3 FontLoader.init() screen mode 서명 비교 무효
- **File**: `src/resource/font-loader.ts:116-125, 240-243`
- **Severity**: Critical
- non-print mode에서 호출자 `fonts` 무시, `_loadServer()` fetch 후 `_lastFontsSignature`를 fetched 배열로 기록. early-exit 검사는 호출자 인자로 `candidateSignature` 계산. 동일 인자 재호출 시 stale.
- **Reproduction**: `init(someFonts)` 후 서버 `fonts.json` 변경, 동일 `someFonts` 재호출 → refetch 안 함.

### D.2 Potential Bugs

#### D.2.1 _computeFontsSignature가 absent vs empty 필드 미구분
- **File**: `src/resource/font-loader.ts:240-243`
- **Severity**: Medium
- `${f.ttfFilename ?? ''}|${f.base64Data ?? ''}`. `{ base64Data: undefined }`와 `{ base64Data: '' }` 동일. low impact.

#### D.2.2 FontLoaderEngineImpl.getParsedFont 첫 parsed font fallback
- **File**: `src/engine/font-loader-engine.ts:111-122`
- **Severity**: Medium
- `fontName` 생략 시 `_parsedFonts.values()` 첫 entry 반환. 첫 input font가 `ttfFilename`만 있고 parse 실패 시, 두 번째 font가 default로.
- **Reproduction**: `[{family:'A', ttfFilename:'a.ttf'}, {family:'B', base64Data:'...'}]` → `getParsedFont()`이 B 반환, A가 default 기대.

#### D.2.3 clampStaticToContainer 분수 line count
- **File**: `src/utils/static-grid-containment.ts:89-110`
- **Severity**: Medium
- `containerLineCount = Math.floor(...) + 1`. 분수 `editableHeight / lineHeight` 시 마지막 partial line 허용이 기하학적으로 불일치.
- **Reproduction**: `containerLineCount=57.708`, `top=56, height=2` → `{top:56.708, height:1}`.

#### D.2.4 clampStatic/AbsoluteToContainer missing model fallback
- **File**: `src/utils/static-grid-containment.ts:96-97, 142-143`
- **Severity**: Medium
- `container.model` undefined 시 `{left:0, top:0, width:1, height:1}` 반환, 에러 없음.
- **Reproduction**: detached `LayoutBoxElement`에 clamp 호출 → 1×1 at origin.

#### D.2.5 valueEqual NaN/중첩 배열 미처리
- **File**: `src/utils/value-equal.ts:17-31`
- **Severity**: Low/Medium
- `valueEqual(NaN, NaN)` → `false`(`===`). `valueEqual([1, [2, 3]], [1, [2, 3]])` → `false`(primitive만 비교).
- **Reproduction**: Node 스크립트로 확인. 유일 caller `paragraph.element.ts`가 `column`/`gap` 숫자 배열 비교, NaN/중첩 미예상.

### D.3 Band-Aid/Brittle Code

#### D.3.1 random.ts as any / require workaround
- **File**: `src/utils/random.ts:11-12, 23, 30-34`
- **Severity**: Low
- `(globalThis as any).crypto`, `(window as any).crypto`, `randomBuffer[0] as number`, 조건부 `require` try/catch + eslint-disable. bundler/SSR compat band-aid.

#### D.3.2 ColorRegistry 생성자 window.matchMedia("print") import time 캡처
- **File**: `src/resource/color-registry.ts:47-49`
- **Severity**: Low
- singleton 생성자에서 mode capture. media query 변경 시 미감지. `_isPrint` override setter 없음. `FontLoader.ts:58-60` 동일. 단위 테스트 시 모드 전환 불가.

#### D.3.3 FontLoader getFontFamily throw vs getParsedFont null
- **File**: `src/resource/font-loader.ts:216-258`
- **Severity**: Low
- `getFontFamily()` throw, `getParsedFont()` return null. inconsistent failure modes.

#### D.3.4 FontLoader.init() document.fonts.clear() before validation
- **File**: `src/resource/font-loader.ts:127`
- **Severity**: Low
- `globalThis.document?.fonts.clear()`가 try block 전. `_loadServer()`/`_parseFonts()` throw 시 document에 font 없음. atomic 아님.

### D.4 Dead/Unused Code

#### D.4.1 src/utils/lru-cache.ts export LRU 사용처 없음
- **File**: `src/utils/lru-cache.ts`, `src/utils/index.ts:5`
- **Severity**: Low
- grep + codegraph_callers 0건. `ParagraphEngine`이 자체 private `_LRU` 사용.

#### D.4.2 registerLoader/resetLoader 호출자 없음
- **File**: `src/resource/color-registry.ts:19, 69-78`, `src/resource/font-loader.ts:21, 80-89`
- **Severity**: Low
- public API hook이나 내부/React caller 없음. 미테스트.

#### D.4.3 genRandom은 genUUID 내부만
- **File**: `src/utils/random.ts`, `src/utils/gen-uuid.ts:1`
- **Severity**: Low
- `genRandom`이 `gen-uuid.ts`만 import. `index.ts`에서 re-export 안 됨. 무거운 compat 코드가 단일 4-digit random용.

### D.5 Incomplete Implementations

#### D.5.1 ImageData type docs가 objectFit "UI 메타데이터 ONLY" 명시, 실제 렌더링 사용
- **File**: `src/types/layout/image.type.ts:4-15, 157-166` vs `src/components/layout/image.element.ts:478, 484-486, 449-455`
- **Severity**: Medium
- type JSDoc이 "LayoutImageElement 렌더링 시에는 objectFit을 참조하지 않는다" 명시. 코드가 `computeObjectFit()` 호출. 또한 `computeObjectFit`이 `fit === 'none'` 시 `{x:0, y:0, width:originalWidth, height:originalHeight}` 반환, docs가 mm-based display position 명시와 불일치.

#### D.5.2 PrintPostData generic default union이 element kind 상실
- **File**: `src/types/print/post-data.type.ts:77`
- **Severity**: Low
- `PrintPostData<T = BoxData | ImageData | ...>` default union. caller가 generic 생략 시 `data.type`으로 narrow 안 됨.

#### D.5.3 PlaceGunItem.content union에 discriminator 없음
- **File**: `src/types/edit/place-gun.type.ts:116-132`
- **Severity**: Low
- `content: ArticleContent | ImageContent | ...`가 `contentType`과 관계 없음. consumer가 cast 필요.

#### D.5.4 TableCellSelection.selectMode에 docs 미기재 값
- **File**: `src/types/edit/table-selection.type.ts:24, 33-39`
- **Severity**: Low
- `selectMode?: 'cell' | 'row' | 'col'`. AGENTS.md는 F5/F7/F8 cell block만 기재, `'row'`/`'col'` 미기재.

### D.6 Documentation/Code Drift

#### D.6.1 docs/RESOURCE.md screen mode ttfFilename vs base64Data 우선
- **File**: `docs/RESOURCE.md` vs `src/resource/font-loader.ts:147-150`
- **Severity**: Medium
- docs가 screen mode `ttfFilename` 사용 암시. 코드는 `base64Data` 우선(양 모드). AGENTS.md는 정확, standalone resource doc stale 가능.

#### D.6.2 docs/API.md objectFit UI-only
- **File**: `docs/API.md` vs `src/types/layout/image.type.ts:4-15`
- **Severity**: Medium
- type doc이 `objectFit`을 "LayoutImageElement 렌더링 미사용" 명시. `image.element.ts`가 의존.

#### D.6.3 docs/RESOURCE.md _defaultColor 예제 모순
- **File**: `docs/RESOURCE.md:335, 343, 370` vs `src/resource/color-registry.ts:139`
- **Severity**: Low
- table이 `{c:0,m:0,y:0,k:0}`, sequence diagram이 `{c:0,m:0,y:0,k:255}`. 코드는 init 후 `{c:0,m:0,y:0,k:255}`. table 오류.

#### D.6.4 TextStyle spaceRatio default 0.5 vs 0.15 충돌
- **File**: `src/types/style/text-style.type.ts:22-24, 78`
- **Severity**: Low
- line 24이 "기본값 0.5" 명시, line 78이 `0.15` default. 동일 파일 내 충돌. git log에 "0.25 → 0.5 변경" commit 있으나 line 78이 stale.

---

## 부록: 감사 에이전트 메타데이터

| 에이전트           | bg ID           | 소요 시간 | 담당                                                                                       |
| ------------------ | --------------- | --------- | ------------------------------------------------------------------------------------------ |
| Engine audit       | `bg_f0520fdd`     | 4m 55s    | `src/engine/` 13개 파일, docs/ENGINE.md, TEXT_ENGINE.md, PERFORMANCE.md 교차 검증             |
| Edit audit         | `bg_ce88dfba`     | 10m 9s    | `src/edit/` 11개 파일, docs/EDITING_*.md 교차 검증                                            |
| Components audit   | `bg_c6329de2`     | 6m 49s    | `src/components/` 14개 파일, AGENTS.md, docs/TEXT_ENGINE.md, PERFORMANCE.md, API.md 교차 검증 |
| Integration audit  | `bg_19a90ea4`     | ~15m      | engine↔DOM seam, 양쪽 file:line 인용                                                          |
| Resource/utils audit | `bg_8ef73e50`     | 12m 40s   | `src/resource/`, `src/utils/`, `src/types/` 전체, docs/RESOURCE.md, API.md 교차 검증           |

---

## 끝

본 보고서는 2026-08-19 기준 코드베이스 상태를 기반으로 작성되었습니다. 모든 발견사항은 감사만 수행, 코드 수정 없음. 수정 우선순위 결정의 출발점으로 활용하시기 바랍니다.